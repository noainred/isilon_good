# CLAUDE.md — 이 저장소에서 일하는 방식

## 대화 원칙 (사용자와 합의, 2026-06-12) — 반드시 지킬 것
- **사실과 팩트에 기반해 정직하게** 말한다. 모르면 모른다고, 틀렸으면 틀렸다고 한다.
- **자존심·고집으로 우기지 않는다.** 목표는 세상을 개선하는 것이지 논쟁에서 이기는 게 아니다.
- 측정으로 확인할 수 있으면 추측하지 말고 **측정**한다. 결과를 **과장하지 않는다**.
- 사용자에게 듣기 좋은 말보다 **정확한 말**을 우선한다. (이게 사용자를 돕는 길이다.)
- **모든 답변은 한글로 한다(사용자 요청, 2026-06-14).** 사용자가 더 빨리 이해한다.
  files/s·pscan 같은 기술 용어는 그대로 둬도 되나, 문장(설명)은 반드시 한글로 쓴다.
- **사용자가 `.`(점)만 보내면 "안녕"(내가 살아있는지 확인하는) 인사다(2026-06-14).**
  큰 작업을 추측해 시작하지 말고, 가볍게 인사로 답하고 다음 지시를 기다린다.
- **‘Nexus 화면’(Nexus Repository 통합 관리)을 가져오면 “여기(isilon_usage 저장소)가 아니다”라고 답하고,
  바로 처리하지 않는다(사용자 요청, 2026-06-19).** 코드를 건드리기 전에 **“이걸 처리할까요?”라고 먼저 물어본다.**
  (포탈 코드에 비슷한 문자열이 있어도 Nexus 앱은 별개다 — 임의로 손대지 말 것.)

## 프로젝트(isilon_usage) — 측정으로 확정된 핵심 사실 (재논의·재추측 금지)
- 거대 아이실론 스캔의 병목은 **DB 크기도 NAS 대역폭도 아니다.** strace/gdb 진단 결과
  **GIL + 단일 `_dlock` + 단일 DB 커넥션 직렬화**가 원인(워커 8개가 사실상 1~2개 속도, ~1,000 stat/s).
- **fold-depth = DB 크기/행 수 묶기**(속도 직결 아님). fold 도 용량을 세려고 파일을 stat 한다.
  깊은 중첩 트리엔 디렉터리 수를 줄여 도움, 납작한 거대 디렉터리(한 폴더에 파일 수백만)엔 효과 거의 없음.
- **pscan(멀티프로세스) = 처리량 처방.** PoC 실측: 같은 작업에서 스레드 1→8 = 0.78배(GIL 정체),
  프로세스 1→8 = 3.75배(4코어 선형), 4워커 기준 프로세스가 스레드 5.48배.
- 6 PB 규모의 진짜 답은 **아이실론 네이티브(SmartQuotas 어카운팅 쿼터 · FSA/InsightIQ)** —
  파일을 걷지 않고 클러스터 내부 메타데이터로 조회. (실 클러스터 없으면 미검증.)
- DB 엔진 교체 / 저장형식(CSV 등) 변경은 **측정상 무의미**(쓰기는 한가했다).

## 설계 원칙 (사용자 강조, 2026-06-18) — 반드시 지킬 것
- **아키텍처는 일관된 정책으로 간다.** 같은 데이터·기능을 화면마다 **복제하지 말고**
  단일 소스(요소·상태·정책) 하나를 공유한다. 복제하면 동기화가 갈라져 값이 어긋난다.
- 예: 스캔 선택기(`scanSelectCard`)·진행 중 배너(`analysisNotice`)는 하나만 두고
  활성 뷰로 옮겨 보여준다(`mountScanControls`) — ‘디렉터리 분석’·‘분석 리포트’가 같은 선택·같은 배너를 쓴다.
- 새 화면/기능을 붙일 때 기존 것과 **데이터·라벨·동작이 통일**되는지 먼저 확인한다(통일된 데이터로 간다).

## 운영
- 작업 브랜치: `claude/upbeat-bell-cXX8f`. 푸시는 항상 이 브랜치로.
- 릴리스: `python3 tools/make_release.py` → `download/`(버전별 + latest, tar.gz/zip).
- 변경 시 `CHANGELOG.md`·`docs/PROMPT_HISTORY.md` 갱신, 테스트(`tests/test_*.py` 직접 실행)+ruff 통과 확인.
