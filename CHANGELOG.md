# 변경 이력 (릴리즈 노트)

이 프로젝트의 모든 주요 변경 사항을 이 파일에 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 를 따르고,
버전은 [유의적 버전(SemVer)](https://semver.org/lang/ko/) 을 따릅니다.

버전 표기: `MAJOR.MINOR.PATCH`
- MAJOR — 호환되지 않는 변경
- MINOR — 호환되는 기능 추가
- PATCH — 호환되는 버그 수정

DB 스키마 버전은 각 DB 의 `PRAGMA user_version` 에 기록되며, 현재 스키마 버전은 **9** 입니다.

---

## [1.69.0] - 2026-06-15

### 추가됨 (Added) — 원격 자동 구성: 기존 노드 '덮어쓰기'(중복 방지 + 토큰 보존)

'노드 설정 → 설치·구성 → 원격 자동 구성' 에서 노드를 등록할 때, **같은 노드(같은 이름, 또는
같은 서버 `IP:포트`)가 이미 있으면 전체 덮어쓰기**하도록 했다(체크박스 **'기존 노드 덮어쓰기'**,
기본 ON).

- 같은 서버가 **다른 이름**으로 등록돼 있던 **중복 항목도 제거**(URL 매칭).
- **API 토큰을 비우면** 기존 노드의 토큰을 **재사용**(엣지 페어링 유지) — 재등록만으로 토큰이
  바뀌어 401 나던 일을 막는다. 토큰을 직접 입력하면 그 값으로 교체(생성 스크립트로 엣지에 적용).
- 체크를 끄면 이미 있을 때 등록하지 않고 사유를 돌려준다.
- 결과 메시지에 **덮어씀/신규 · 기존 토큰 유지/새 토큰** 을 표시. 회귀 테스트 + HTTP 스모크 검증.

---

## [1.68.2] - 2026-06-15

### 수정됨 (Fixed) — 업그레이드 중복 실행(자동+수동 동시 푸시) 차단

진행 팝업 로그에서 **`③ 엣지 전파 시작` 이 두 번** 찍히고 한 엣지가 `Broken pipe` 로 실패하던
원인: **인터넷 자동 업그레이드(백그라운드)와 수동 '지금 업그레이드' 가 동시에** 코드를 받아
엣지에 푸시·재시작하고 있었다(같은 번들을 2번 전송 → 재시작 중인 엣지로 깨진 연결). 두 경로를
**단일 뮤텍스(`_try_begin_install`, 원자적 test-and-set)** 로 직렬화한다:

- `_check_self_upgrade`(자동)는 이미 설치 중(`installing`)이면 건너뛰고, 설치 시작 시 잠금을 잡는다.
- 수동 `upgrade_install` 도 같은 잠금을 잡으며, 못 잡으면 `이미 설치 중` 을 돌려준다.
- 재시작 직전까지 잠금을 유지(1.5초 틈새 중복도 차단). 모달은 `이미 설치 중` 이면 **진행 중인
  설치를 그대로 폴링**해 보여준다(중복 시작 안 함). 회귀 테스트(뮤텍스) 추가.

> 참고: 로그의 `192.168.139.20 → 거부됨 — 엣지에 api_token 설정 필요` 는 **별개의 설정 문제**다 —
> 그 엣지에 `api_token` 을 설정하고 포탈 노드의 토큰을 맞추거나, `--hq` 로 다시 자기등록하면 된다.

---

## [1.68.1] - 2026-06-15

### 추가됨 (Added) — 포탈 로그인 비밀번호 '해제' 버튼 + 분실 복구 안내

포탈 '노드 설정 → 🔒 보안·감사' 에 **'🔓 비밀번호 해제'** 버튼을 추가했다(기존엔 칸을 비우고
저장해야 해서 잘 안 보였다). **로그인 상태**에서 누르면 보호가 풀린다(현재 비밀번호를 아는
사람만 해제 — 무인증 해제는 401). 비밀번호를 **잊었을 때**의 복구법(데이터 폴더
`portal_settings.json` 의 `op_password` 를 `""` 로 바꾸고 재시작)도 화면에 안내한다. (엣지
대시보드는 원래 '작업 보호 비밀번호' 체크를 해제·저장하면 풀린다.)

### 문서 (Docs) — 한 줄 설치·자기등록·오프라인 업그레이드를 가이드에 반영

`README`·`INSTALL`·`GETTING_STARTED`·`USER_GUIDE` 에 **한 줄 설치(`curl|bash`)**, GitHub
토큰(비공개 저장소), **엣지 자기등록(`--hq`/`--enroll`)**, **포탈 오프라인 업그레이드(릴리스
폴더)**, **업그레이드 진행 팝업**, 서비스명(`isilon-edge`/`isilon-portal`)을 반영했다. 정식 한 줄
명령은 `download/README.md` 한곳으로 통일하고, 나머지 문서는 요약 + 링크로 정리(중복·드리프트 방지).

---

## [1.68.0] - 2026-06-15

### 추가됨 (Added) — 업그레이드 진행 팝업(단계별 라이브 로그)

`지금 업그레이드(+전 엣지)` 가 "설치·전파 중…"만 띄워 **아무것도 안 하는 것처럼** 보이던 걸,
**모달 팝업으로 단계별 진행을 라이브로** 보여주게 했다: ① 최신 코드 내려받기 → ② HQ 코드
교체 → ③ 엣지별 전파(엣지마다 ✓/✗) → ④ 재시작 → ✅ 새 버전 복귀.

- `upgrade_install` 을 **백그라운드 비동기**로(즉시 반환), `_do_upgrade_install` 이 단계마다
  `_upg_log` 를 남긴다. `push_upgrade_all` 도 **엣지별 성공/실패**를 로그에 남긴다.
  `upgrade_status` 에 `install_started`·`install_done`·`install_error`·`target` 노출.
- 포탈 UI: 업그레이드 모달이 `/api/portal/upgrade/status` 를 **0.7초마다 폴링**해 로그를
  색으로 렌더하고, **재시작 공백(연결 끊김)** 을 `🔄 재시작 중` 으로 표시한 뒤 새 버전으로
  복귀하면 `✅ 완료` 로 마친다(닫아도 백그라운드로 계속). 회귀 테스트 + HTTP 스모크 검증.

---

## [1.67.2] - 2026-06-15

### 수정됨 (Fixed) — '엣지 모두 최신' 착시: 엣지 버전 뒤처짐을 업그레이드 상태에 표시

업그레이드 패널의 `✓ 최신 · 엣지 N대` 는 **HQ 자신**(현재 vs GitHub 최신)만 보고, 등록된
엣지들이 HQ보다 낮은 버전인지는 보지 않았다. 그래서 한 노드가 구버전(예: HQ 1.67.1인데
노드 1.67.0)이어도 **'모두 최신'처럼** 보였다. 이제 `upgrade_status` 가 등록 노드들의 캐시된
버전을 HQ(현재) 버전과 비교해 **`edges_outdated`(구버전 수)·`edges_outdated_list`(노드
id·버전)·`edges_unknown`(버전 미확인)** 을 함께 반환하고, 패널이 **`⚠ N대 구버전 (id vX …)`**
또는 `엣지 모두 최신` 으로 정직하게 표시한다. (노드 표의 버전·⬆ 배지는 종전대로.) `_ver_tuple`
신설, 회귀 테스트 + HTTP 스모크 검증.

> 노드가 '구버전'으로 남는데 푸시가 성공으로 보였다면, 그 엣지가 **다른 코드 디렉터리에서
> 돌거나 재시작이 안 된** 경우다 — 해당 엣지에서 설치/업그레이드 한 줄을 다시 실행하면 된다.

---

## [1.67.1] - 2026-06-15

### 변경됨 (Changed) — systemd 서비스/유닛 이름을 isilon-edge / isilon-portal 로 통일

packaging 유닛 템플릿과 운영 문서가 옛 서비스명(`isilon_usage`·`isilon_usage_portal`,
일부 `isilon-usage`)을 써서 `install_edge.sh`(`isilon-edge`)·포탈 생성 스크립트와 어긋났다.
그 결과 업그레이드/재시작이 **엉뚱한 유닛을 가리켜 서비스 장애**가 났다. **서비스/유닛/문서
이름만** `isilon-edge`/`isilon-portal` 로 통일한다. **파이썬 패키지(`isilon_usage`)·릴리스
파일명(`isilon_usage-*.tar.gz`)·agent-bundle·기존 데이터 경로(`/data/isilon_usage`)는 호환을
위해 그대로** 둔다(`ExecStart=python3 -m isilon_usage …` 유지).

- `packaging/isilon_usage.service` → **`packaging/isilon-edge.service`**(서비스 `isilon-edge`)
- `packaging/isilon_usage_portal.service` → **`packaging/isilon-portal.service`**(서비스 `isilon-portal`)
- `docs/SERVICE.md`·`USER_GUIDE.md`·`INSTALL.md` 의 `systemctl`/`journalctl`/유닛 파일명/
  서비스명 참조를 새 이름으로(콘솔 명령 `isilon-usage`(pip 진입점)·패키지 디렉터리 언급은 유지).

> 이미 배포된 서버는 유닛 파일이 디스크에 남아 있으므로, 옛 `isilon_usage(.service)` 유닛을
> 멈춰 비활성화한 뒤 새 `isilon-edge`/`isilon-portal` 로 재설치(또는 유닛 파일명 변경)해야 한다.

---

## [1.67.0] - 2026-06-15

### 추가됨 (Added) — 엣지 오프라인 업그레이드: 포탈 릴리스 폴더에서 엣지가 한 줄로 당겨감

포탈 서버의 **릴리스 폴더**(설정 `release_dir`, 기본 `/opt/isilon_release`)에
`isilon_usage-*.tar.gz` 를 두면, 각 엣지가 **인터넷·GitHub·토큰 없이** 포탈에서 받아
업그레이드한다(엣지 콘솔에서 한 줄). 포탈→엣지 푸시와 달리 **엣지가 당겨가므로** 노드
등록·인바운드 접근이 필요 없다.

- 포탈 `GET /api/portal/release` — `release_dir` 의 **가장 최신 버전** tarball(파일명 버전 기준,
  못 읽으면 mtime)을 application/gzip 으로 서빙. 없으면 404. `GET /api/portal/release/info`
  는 현재 폴더·최신 파일명을 JSON 으로.
- 엣지 한 줄: `curl -fsSL http://<HQ>:8800/api/portal/release | sudo tar -xz -C /opt/isilon_edge
  --strip-components=1 && (cd /opt/isilon_edge && python3 -m isilon_usage --version)
  && sudo systemctl restart isilon-edge`.
- 포탈 '노드 설정 → 업그레이드' 탭에 릴리스 폴더 지정 UI + 현재 패키지 표시 + 엣지 한 줄
  자동 생성(포탈 주소 채워 클릭 복사).
- `newest_release_archive()` / `set_release_dir()` / `release_info()` 신설, `upgrade_config`
  에 release 정보 노출. 회귀 테스트(버전 선택)+HTTP 스모크(404→최신 200) 검증.

---

## [1.66.0] - 2026-06-15

### 추가됨 (Added) — 엣지 자기등록(enroll): 설치 한 줄로 포탈에 자동 등록

엣지에서 설치 한 줄에 **`--hq http://<HQ-IP>:8800`** 을 주면, 설치·기동 직후 엣지가
포탈에 **스스로 등록**한다(포탈에서 IP 입력하는 방향의 반대). 엣지가 자기 URL과 **실제
api_token** 을 함께 보내므로 포탈 폴링이 곧바로 인증된다 — 토큰을 따로 맞출 필요가 없어
**401 Unauthorized 도 원천 예방**된다.

- 포탈 `POST /api/portal/enroll`(인증 게이트 앞 — 엣지는 포탈 로그인 토큰이 없으므로):
  `{id,url,token,region,path,enroll_token}` 를 받아 노드를 upsert.
- **인증**: 포탈에 `enroll_token`(공유 가입 비밀)이 설정돼 있으면 일치해야 한다
  (`secrets.compare_digest`). 미설정이면 — 포탈에 로그인 비밀번호가 걸려 있을 때는 거부
  (무인증 자기등록 금지), 비밀번호도 없으면 LAN 신뢰로 허용. 인증 실패는 HTTP 401.
- 포탈 '보안·감사' 탭에 **enroll token 설정 UI** 추가(`/api/portal/settings` 로 저장,
  값은 노출하지 않고 설정 여부만 표시).
- `install_edge.sh`: `--hq`·`--region`·`--node-id`·`--enroll`·`--advertise-host` 옵션 추가.
  등록 URL 은 `http://<자동감지 IP 또는 --advertise-host>:<port>`. 등록 실패해도 설치 자체는
  정상 진행하고 수동 등록 안내를 출력한다. JSON 은 python3 로 안전 직렬화.

회귀 테스트(enroll 개방 허용 / enroll_token 불일치 401 / 일치 허용) 추가. HTTP 스모크 검증.

---

## [1.65.2] - 2026-06-15

### 변경됨 (Changed) — 엣지 설치 규칙을 install_edge.sh 로 통일(서비스 `isilon-edge`, `/opt/isilon_edge`)

포탈의 '원격 자동 구성'·'원격 업그레이드' 스크립트와 packaging 유닛 템플릿이 옛 이름
(`isilon_usage` 서비스, `/opt/isilon_usage`, `/data/isilon_usage`)을 써서, `install_edge.sh`
(`isilon-edge`·`/opt/isilon_edge`·`/data/isilon_edge_data`)로 깐 엣지와 **경로/서비스가
어긋났다.** 그 결과 한 서버에 서비스가 둘 생기거나, 새로 만든 토큰이 실제 엣지에 적용되지
않아 포탈 폴링이 **401 Unauthorized** 로 떨어졌다. 사용자 합의(2026-06-15)로 **엣지 규칙을
`isilon-edge` 로 통일**한다:

- `build_provision_script`: systemd 유닛명 `isilon_usage.service` → **`isilon-edge.service`**,
  data-dir `/data/isilon_usage` → **`/data/isilon_edge_data`**, `enable --now`/`journalctl`
  대상도 `isilon-edge`. (코드 디렉터리는 이미 `/opt/isilon_edge`.)
- `build_upgrade_script`·`push_upgrade_all_ssh`·`upgrade_plan`: 기본 서비스명
  `isilon_usage` → **`isilon-edge`**.
- packaging 유닛 템플릿(`isilon_usage.service`·`isilon_usage_portal.service`)과
  `docs/SERVICE.md` 의 `/opt/isilon_usage` → **`/opt/isilon_edge`**.
- 회귀 방지: 생성 스크립트가 `isilon-edge.service`·`/data/isilon_edge_data` 를 담고
  옛 `isilon_usage.service` 가 없음을 테스트로 고정.

> 참고: 과거 기록(CHANGELOG/PROMPT_HISTORY)의 `/opt/isilon_usage` 언급은 그 시점 사실이라
> 보존한다. manager 기본 data-dir(`/data/isilon_usage`)·packaging 유닛의 `--data-dir`·서비스
> 파일명은 이번 범위(엣지 `/opt` 경로)에 포함하지 않았다(필요 시 별도 통일).

---

## [1.65.1] - 2026-06-15

### 수정됨 (Fixed) — 설치/업그레이드 스크립트가 비공개(private) 저장소에서도 받도록

`install_edge.sh`·`install_portal.sh` 가 인증 없이 `github.com/.../raw/…` 만 받아, **비공개
저장소**에서는 `versions.json` 이 404/로그인 HTML 로 와서 "최신 버전 읽기 실패"로 죽었다
(증상: "1줄 업그레이드 스크립트가 동작하지 않음"). 이제:

- `--token <GitHub PAT>`(또는 환경변수 `GITHUB_TOKEN`) 를 주면 **GitHub API(contents, raw)**
  로 토큰 인증 다운로드한다(공개 저장소면 종전대로 raw URL). 헤더 인증이라 슬래시 포함
  브랜치명도 안전.
- 받은 내용이 올바른 JSON 이 아니면(=비공개로 막힘) 일반 실패 대신 **친절한 안내**를 출력
  한다("비공개 저장소면 --token <PAT> 로 다시 실행").
- 사용법/도움말(`--help`)·시작 배너에 인증 모드(토큰/공개) 표기 추가.

> 패키지(파이썬) 코드는 1.65.0 과 동일하며, 번들된 설치 스크립트만 갱신된 패치 릴리스다.

---

## [1.65.0] - 2026-06-15

### 변경됨 (Changed) — '지정 경로'를 하드 차단이 아닌 '소프트 컨펌' 가드로

엣지 새 스캔의 마운트 경로 제한(`mount_bases`)을 **기본값/가드**로 바꿨다. 지금까지는 지정
경로 밖이면 `not_allowed` 로 **무조건 차단**이라, 예컨대 `/mnt/isilon` 이 지정돼 있어도 사소한
공백/구버전 등으로 하위 경로 탐색이 막히는 일이 있었다. 이제는:

- 탐색/스캔 대상이 지정 경로 **밖**이면 차단 대신 `outside_base` 신호를 주고, 대시보드가
  **"지정한 경로를 벗어났습니다 — 이 경로로 진행할까요?"** 컨펌을 띄운다. 확인하면 그 경로로
  탐색·스캔을 **진행**(서버는 `confirm`/`confirm_outside` 플래그로 허용)한다.
- 폴더 탐색은 한 번 컨펌하면 그 세션 동안 자유 탐색(매번 묻지 않음), '위로'도 풀린다.
- **예약 스캔**은 관리자가 미리 지정한 경로라 컨펌 없이 그대로 진행한다.
- 디렉터리가 아니거나 접근 불가한 경로는 (되돌릴 수 없으므로) 여전히 하드 거부한다.
- 보안 주의: 지정 경로 밖 탐색/스캔을 사용자가 컨펌으로 넘길 수 있게 됐으므로, 신뢰할 수
  없는 망에 무인증으로 노출하는 배치라면 로그인(비밀번호)을 함께 켜는 것을 권장한다.

서버: `ScanController.scan_path_check()` 신설(존재 검사=하드 / 지정경로 밖=소프트), `/api/browse`
(`?confirm=1`)·`start_scan`·`autotune_start`·`/api/scan/start`·`/api/autotune/start` 에
`confirm_outside` 연결.

### 추가됨 (Added) — 포탈 감시 폴더 '📁 찾아보기'(서버 디렉터리 브라우저)

포탈의 '자동 업그레이드 → 감시 폴더' 입력칸 옆에 **📁 찾아보기** 버튼을 추가했다. HQ(포탈)
서버의 로컬 디렉터리를 모달에서 클릭으로 들어가며 골라 그대로 입력칸에 채운다. 엣지의
폴더 탐색을 재사용한 `GET /api/portal/browse`(`browse_dir`) 신설.

### 수정됨 (Fixed) — 엣지 설치 스크립트가 api_token 을 자동 생성·유지 (install_edge.sh)

포탈의 '전 노드 지금 업그레이드'(푸시)는 엣지에 `api_token` 이 있어야 한다(없으면 403 —
`거부됨 — 엣지에 api_token 설정 필요`). 지금까지 `install_edge.sh` 는 토큰을 설정하지 않아
설치 직후 푸시가 거부됐다. 이제 설치/업그레이드 시 `--api-token` 값(없으면 기존 유지, 그것도
없으면 자동 생성)을 `settings.json` 에 보존하고 **완료 화면에 토큰을 출력**한다. 그 토큰을
포탈 '노드 설정'에 넣으면 푸시 업그레이드가 동작한다.

---

## [1.64.3] - 2026-06-15

### 추가됨 (Added) — 포탈(HQ) 자동 설치/업그레이드 스크립트 (tools/install_portal.sh)

엣지 스크립트와 동일 구조의 **HQ 통합 포탈용**. 같은 패키지에 포탈이 포함돼 있어 서비스만
분리한다 — `/opt/isilon_portal` 설치, `/data/isilon_portal_data` 데이터, `/tmp/isilon_portal`
임시 staging, 포트 8800, **systemd `isilon-portal` 등록·재시작**(ExecStart=`… portal …`).
스캔을 안 하므로 `--mount-base` 없음. 다시 실행하면 업그레이드.

---

## [1.64.2] - 2026-06-15

### 변경됨 (Changed) — 엣지 설치 스크립트: 임시 작업 디렉터리(/tmp/isilon_edge) staging

`install_edge.sh` 가 다운로드한 패키지를 **`/tmp/isilon_edge` 에 먼저 풀어 검증**한 뒤, **통과분만**
설치 경로 `/opt/isilon_edge` 로 반영한다(rsync 있으면 `--delete`, 없으면 `cp -a`). 검증(`--version`)
실패 시 **기존 설치를 그대로 보존**하고 중단해 안전하다. `--tmp-dir` 옵션 추가.

---

## [1.64.1] - 2026-06-15

### 추가됨 (Added) — 엣지 자동 설치/업그레이드 스크립트 (tools/install_edge.sh)

최신 패키지를 인터넷에서 받아 한 번에 설치·서비스 등록·재시작하는 bash 스크립트.

- **다운로드** `/opt` 에 최신 `isilon_usage-<버전>.tar.gz`(versions.json 의 latest) → **설치**
  `/opt/isilon_edge`(`--strip-components=1`) → **데이터** `/data/isilon_edge_data` 생성 →
  **검증**(`--version`) → **systemd 서비스 `isilon-edge` 등록 → 재시작**.
- 다시 실행하면 **최신으로 교체 후 재시작(업그레이드)**. 옵션: `--port`·`--mount-base`·`--branch`·
  `--data-dir`·`--install-dir`. root 필요, curl/wget 자동 감지, 손상 파일 검사.

---

## [1.64.0] - 2026-06-15

### 추가됨 (Added) — 포탈 핑 차트 인터랙션 + 엣지 스캔 이력 별도 메뉴

- **포탈 인프라 체크 핑 차트** — ① 점에 **마우스오버 → 시간·ping(ms) 툴팁** ② **더블클릭 → 확대
  모달**(1년치 재조회해 큰 차트) ③ 모달에서 **드래그=시점 이동 · 휠=확대/축소 · 더블클릭=리셋**.
  작은 카드와 큰 모달이 `_drawSpark` 공통 렌더를 쓰고, 보이는 시간 윈도우의 점만 그린다.
- **엣지 대시보드** — '스캔 이력(회차별 소요 시간·결과)' 표를 Summary에서 떼어 **별도 탭
  「📋 스캔 이력」** 으로 분리(네비/뷰/VIEWS 등록).

---

## [1.63.0] - 2026-06-14

### 추가됨 (Added) — 분석 리포트에 '파일 크기별 분포'

나이/소유자/확장자와 똑같은 방식으로 **파일 크기 버킷별 파일 수·용량**을 집계한다. 작은 파일이
많으면 메타데이터 부담(스캔도 느려짐), 큰 파일은 정리 1순위 식별에 쓴다.

- 버킷: `0(빈 파일) / 1B~1KB / 1KB~1MB / 1~10MB / 10~100MB / 100MB~1GB / 1~10GB / 10GB+`.
- **scanner**: `_size_bucket` + 스레드 로컬/공유 집계(_local_accum·_accum_stats·_accum_merge·
  _write_stats·_load_stats). 키는 **파일 원본 크기**(하드링크 dedup 무관), bytes 는 dedup 반영.
  → scan_stats `kind="size"`. 정합성 테스트 통과.
- **API** `/api/stats` 에 `sizes`(버킷 순 정렬). **대시보드 「📊 분석 리포트」에 📏 크기별 분포
  카드**. **CLI `stats`** 에도 출력.
- 스키마 변경 없음(scan_stats kind 추가만). 새 스캔부터 채워짐.

---

## [1.62.0] - 2026-06-14

### 추가됨 (Added) — 인터넷 자동 업그레이드 4단계(완성): 포탈 인터넷 소스 + 엣지 전파 + UI

포탈(HQ)도 엣지처럼 인터넷(GitHub)을 모니터링해 자가 업그레이드하고, 받은 새 버전을 **등록된
모든 엣지에 전파**한 뒤 재시작한다(기존 `push_upgrade_all` 재사용). 이로써 1~4단계 완성.

- 포탈 `_check_self_upgrade` 에 인터넷 소스 추가, `_apply_self_upgrade`(자가→엣지 전파→재시작),
  `upgrade_status/check/install(propagate)`·`set_upgrade_net`. 설치 시 push_upgrade_all 로 전 엣지 전파.
- API: `GET /api/portal/upgrade/status`, `POST /api/portal/upgrade/check·install·net`. 설정
  `upgrade_source/upgrade_url/upgrade_auto`(포탈 설정 파일).
- 포탈 **「노드 설정 → 🔄 업그레이드」 탭에 🌐 인터넷 자동 업그레이드 카드** — 현재/최신 버전
  (업데이트 가능 배지)·확인 시각·**엣지 수**·로그 + 설정(소스/자동/주기/URL) + 버튼(지금 확인 ·
  **지금 업그레이드(+전 엣지)**).
- HTTP 스모크·서빙 검증. **자동 업그레이드 전체 완성**: ① 원격 코어 ② 엣지 체커/API ③ 엣지 UI
  ④ 포탈 인터넷+전파+UI.

---

## [1.61.0] - 2026-06-14

### 추가됨 (Added) — 인터넷 자동 업그레이드 3단계: 엣지 대시보드 상세 UI

엣지 대시보드 **「📖 버전 기록」 탭 맨 위에 ⬆ 자동 업그레이드 카드**를 추가 — 사용자가 요청한
"최대한 상세한 정보"를 한 화면에서 보고 조작한다.

- **상태 표시**: 현재 버전 / 최신 버전(**⬆ 업데이트 가능** 또는 ✓ 최신 배지), 마지막 확인 시각,
  소스 URL, 확인 오류, **실시간 로그**(최근 동작 시간순).
- **설정**: 소스(끔 / GitHub 인터넷 모니터링) · 자동 설치 체크 · 확인 주기(초) → ‘설정 저장’.
- **조작**: **🔄 지금 확인**(인터넷에서 최신 버전 조회) · **⬆ 지금 업그레이드**(업데이트 가능 시만
  노출 — 다운로드·설치 후 자동 재시작).
- `/api/upgrade/status`(탭 진입 시 폴링)·`/check`·`/install` 연동. JS 문법·마커·서빙 검증.

다음(4단계): 포탈도 인터넷 소스 + 받은 새 버전을 등록된 엣지에 전파.

---

## [1.60.0] - 2026-06-14

### 추가됨 (Added) — 인터넷 자동 업그레이드 2단계: 엣지 체커 + 상태 + API

엣지(스캐너) 서버가 인터넷(GitHub)을 모니터링해 새 버전을 받아 설치할 수 있게 했다.

- **엣지 체커** — `_upgrade_watch_loop` 이 기존 감시 폴더에 더해 **인터넷 소스**(`upgrade_source=github`)를
  주기적으로 확인한다. 새 버전이면 상태를 기록하고(상세 표시용), `upgrade_auto` 가 켜져 있으면
  **자동 설치 후 재시작**한다(스캔 중이면 보류). 끄면 '업데이트 가능' 알림만.
- **컨트롤러/상태** — `upgrade_status`(현재/최신·확인시각·source·로그), `upgrade_check`(지금 확인),
  `upgrade_install`(지금 다운로드·설치·1.2초 후 재시작). 로그는 최근 60건 보관.
- **API** — `GET /api/upgrade/status`, `POST /api/upgrade/check`, `POST /api/upgrade/install`.
- **설정** — `upgrade_source`(off/github)·`upgrade_url`(빈값=기본 raw GitHub)·`upgrade_auto`.
- HTTP 스모크로 검증(로컬 릴리스 서버 → 현재→최신 감지·로그).

다음(3~4단계): ③ 상세 정보 UI(엣지 대시보드: 현재/최신·변경점·확인시각·진행·로그·버튼) →
④ 포탈 자가 업그레이드 + 엣지 전파.

---

## [1.59.0] - 2026-06-14

### 추가됨 (Added) — 인터넷(GitHub) 자동 업그레이드 1단계: 원격 확인·다운로드·설치 코어

인터넷/Git 을 모니터링해 새 버전을 자동으로 받아 설치하는 기능의 1단계(코어 + 안전장치).

- **upgrade.py 에 인터넷 소스 함수** — `check_remote`(원격 `versions.json` 으로 최신 버전 확인,
  설치는 안 함), `download_archive`(아카이브 파일명·크기 상한 검증 후 다운로드),
  `upgrade_from_remote`(확인→다운로드→설치를 한 번에). `api.github.com` 이 막히는 환경이 많아
  **raw.githubusercontent.com 의 `download/versions.json`** 을 기본 소스로 사용(HTTPS·읽기 전용).
- 기존 안전장치 재사용 — 아카이브/버전 검증, **더 높은 버전만** 적용, 경로 탈출 차단, 기존 코드
  백업/롤백, 크기 상한.
- 로컬 HTTP 서버로 **네트워크 비의존 테스트**(원격 확인·다운로드·설치 1.0.0→9.9.9·최신판정·파일명검증).

다음(2~4단계): ② 체커+API(주기적 자동 확인/설치) → ③ 상세 정보 UI(현재/최신·변경점·확인시각·
진행·로그) → ④ 엣지 업그레이드(포탈이 받아 전 노드 전파).

---

## [1.58.1] - 2026-06-14

### 추가됨 (Added) — CSV 가져오기에 '샘플 CSV 받기' 버튼

- 포탈 노드 관리 CSV 카드에 **⬇ 샘플 CSV 받기** 버튼 — 헤더+예시 행이 든
  `isilon_nodes_sample.csv` 를 내려받아(엑셀 한글 호환 위해 UTF-8 BOM 포함) 값만 채워 다시
  올릴 수 있다. 클라이언트 측 Blob 생성이라 서버 부담 없음.

---

## [1.58.0] - 2026-06-14

### 추가됨 (Added) — 포탈 노드 CSV 일괄 가져오기(다수 서버 한 번에 등록)

여러 엣지 서버를 하나씩 입력하지 않고 **CSV로 한 번에 등록**할 수 있다. 포탈
**「노드 설정 → 🗂 노드 관리 → 📋 CSV로 여러 서버 가져오기」**.

- **백엔드** `PortalController.import_nodes_csv` — 헤더 유연 매핑(`id`·`url` 필수,
  region·token·alias_local·alias_logical·mode 선택, **한글/별칭 헤더**(이름·주소·지역·토큰)도
  허용), 헤더가 없으면 `id,url,region,token,alias_local,alias_logical` 순서로 간주. 같은 id 는
  **수정**, 토큰 빈값은 기존 유지. **행별 추가/수정/오류를 집계**해 반환. URL 스킴(http://) 자동 보정.
  API `POST /api/portal/nodes/import {csv}`.
- **프론트엔드** — 노드 관리 탭에 **붙여넣기 textarea + .csv 파일 업로드 + 예시 채우기** 버튼,
  결과(추가/수정/오류 행)를 표시.
- test_portal 단위 + HTTP 라우트 스모크로 검증. 설치 매뉴얼 7장에 연결.

---

## [1.57.1] - 2026-06-14

### 변경됨 (Changed) — 설치 매뉴얼에 '포탈 중앙 설치 + 자동 배포' 추가

처음 사용자 설치 매뉴얼에, 여러 서버를 한 번에 올리는 운영 방식을 추가했다.

- **docs/INSTALL.md 7장 신규** — 여러 DC면 각 서버에 일일이 설치하지 말고: ① HQ에 **포탈을 한 번
  설치**(`portal`) → ② 포탈 웹 **「노드 설정 → 🚀 설치·구성」** 에서 엣지 **자동 배포**(A 설치
  스크립트 생성·무의존 / B SSH 자동 푸시) → ③ **감시 폴더**로 이후 버전까지 **전 노드 자동 전파**.
- 0번 요약·초보자 가이드(GETTING_STARTED)에서 이 섹션으로 링크.

---

## [1.57.0] - 2026-06-14

### 추가됨 (Added) — 설치 매뉴얼 + 솔루션 제품소개(인쇄용 A4 3페이지)

- **docs/INSTALL.md** — 처음 사용자를 위한 **설치 전용 매뉴얼**: 환경별 설치(온라인 git /
  폐쇄망 압축본 / pip 패키지 / Docker), 설치 확인, 첫 실행, systemd 등록, 설치 문제 해결,
  보안 체크리스트.
- **docs/SOLUTION_BRIEF.html** — **인쇄/PDF 가능한 A4 3페이지 솔루션 제품소개**(외부 CDN 없는
  단일 HTML): ①문제→솔루션·핵심 가치 ②핵심 기능 6선·안전장치·측정 성과 ③아키텍처·도입 효과·
  시작하기. 브라우저에서 **인쇄 → PDF로 저장**하면 그대로 배포 자료가 된다.
- README 상단에 설치 매뉴얼·제품소개 링크 추가.

---

## [1.56.0] - 2026-06-14

### 추가됨 (Added) — 초보자 가이드 신규 + 매뉴얼 전면 갱신(오토튜닝 4단계/마무리)

모든 소스를 분석해 문서를 현행화하고, 처음 사용자를 위한 가이드를 새로 만들었다.

- **docs/GETTING_STARTED.md(초보자 가이드) 신규** — 처음 쓰는 사람이 설치→첫 스캔 결과까지
  그대로 따라 하는 최단 경로: 5분 체험(데모), 실제 NAS 조사(마운트→serve→웹에서 폴더 선택→
  🔬 오토튜닝 자동 시작), 결과 읽는 법, 자주 막히는 곳, 다음 단계.
- **USER_GUIDE.md 갱신** — 버전/스키마 현행화(1.56.0·스키마 9), **4.4 엔진(threads vs pscan)**
  ·**4.5 오토튜닝** 추가, 6장 CLI 에 `pscan`/`autotune`(+bench_walk), 5장 대시보드 오토튜닝
  카드, 헤더에 초보자 가이드 링크.
- **README.md 갱신** — 초보자 가이드 링크, 엔진·오토튜닝 기능 요약, 서브커맨드(pscan/autotune),
  스키마 9.

이로써 오토튜닝 1~4단계(측정 코어 → 서버 통합 → 첫 화면 실시간 UI → 문서) 완료.

---

## [1.55.0] - 2026-06-14

### 추가됨 (Added) — 오토튜닝 3단계: 첫 화면 실시간 진행 카드 + 스캔 시작 트리거

오토튜닝을 대시보드에서 쓸 수 있게 마무리. 새 스캔 시작 시 기본으로 오토튜닝을 돌리고, 첫 화면에
측정 진행을 실시간으로 보여준 뒤 자동으로 본 스캔을 시작한다 — "처음 분석하면 최적 방법을 자동
측정해 시작하고 첫 화면에 실시간 표시"를 완성.

- 새 스캔 폼에 **🔬 오토튜닝(최적 방법 자동)** 체크박스(기본 켜짐). 켜면 엔진/워커를 자동 결정.
- 첫 화면(Summary) **오토튜닝 진행 카드** — 단일 / 멀티프로세스 / 2단 병렬을 각각 측정하며
  files/s·배속을 실시간 표로 보여주고, 무엇을·왜 하는지 설명(신뢰). 완료 시 "○○가 가장 빠릅니다
  → 스캔 #N 자동 시작" 안내 + 그 스캔으로 자동 추적.
- poll 루프에 `/api/autotune/status` 폴링 통합(진행 중이 아니면 카드 숨김).

다음(4단계): 매뉴얼/초보자 가이드 — 오토튜닝 포함해 한 번에.

---

## [1.54.0] - 2026-06-14

### 추가됨 (Added) — 오토튜닝 2단계: 서버 통합(측정 → 본 스캔 자동 시작) + API

1단계 측정 코어(autotune)를 엣지 서버에 통합. 측정이 끝나면 best 설정으로 본 스캔을 자동 시작한다.

- `autotune_start/status/stop`(ScanController) — 백그라운드로 측정(진행 콜백으로 단계별 스냅샷)
  → 완료 시 best 로 본 스캔 자동 시작. **best 가 병렬(P>1)이면 pscan(빠름), 단일이면 threads
  (상세 트리)**. 측정 중지 가능(stop_event).
- `start_scan`/`_launch_pscan` 에 processes/threads 인자 — 오토튜닝이 정한 값으로 본 스캔.
- API: `GET /api/autotune/status`(폴링), `POST /api/autotune/start {path,secs,then_scan}`, `/stop`.
- HTTP 스모크로 검증(측정 3후보 → best → 본 스캔 자동 시작).

다음(3단계): 첫 화면 실시간 진행 카드 + 설명(신뢰). 4단계: 매뉴얼/초보자 가이드.

---

## [1.53.0] - 2026-06-14

### 추가됨 (Added) — 오토튜닝 1단계: 측정 코어 + CLI (자동 최적 procs×threads)

수동으로 bench_walk 를 돌려 영역별 최적 procs×threads 를 고르던 과정을 자동화한다(새 루트
첫 스캔 시 자동 측정→최적 선택→본 스캔 자동 시작의 기반). 이번은 1단계 — 측정 코어 + CLI.

- **pscan 시간상자(`max_seconds`)** — scan_subtree/threaded/parallel_scan 에 절대시각 deadline 을
  전파해 '부분 스캔'으로 files/s 만 잰다(자식 프로세스에 상속). 기본 0=무제한이라 본 스캔 동작
  불변. 정합성은 test_pscan 으로 보장.
- **autotune 모듈** — 실제 엔진(pscan)을 후보 (1,1)/(P,1)/(P,T) 별로 각 N초 측정해 최적
  (procs,threads)를 고른다. 측정이 본 스캔과 같은 엔진이라 결과가 실제와 일치. 진행 콜백으로
  단계별 실시간 보고(대시보드 연동 기반).
- **CLI `autotune <path>`** — 한 명령으로 측정(수동 bench_walk 대체). 후보별 files/s·배속 +
  실측 권장 + 적용 예시 출력.
- 정직성: 같은 영역 연속 측정이라 캐시로 뒤 후보가 유리(상대 비교용). 로컬 빠른 저장소는 병렬이
  손해라 단일/프로세스가 선택될 수 있고 그것도 올바른 결론.

다음(2~4단계): 서버 통합(새 루트 첫 스캔 자동 트리거+자동 시작) → 대시보드 첫 화면 실시간 진행
카드 → 매뉴얼/초보자 가이드.

---

## [1.52.0] - 2026-06-14

### 변경됨 (Changed) — pscan 적응형 깊이 분할(자식 적은 영역도 병렬화)

실측: 같은 cold 느린 영역이라도 **1단계 자식 수**에 따라 병렬 효율이 갈렸다 —
recycle_bin_gpdb(자식 56개)는 procs8×thr8 = serial 대비 **13.04배**인데, user(자식 16개)는
**1.49배**에 그쳤다. 원인은 NAS 포화가 아니라 **병렬 단위 부족(작업 불균형)**: pscan/bench 가
1단계 자식으로만 프로세스를 쪼개서 자식 적은 영역은 프로세스를 못 채웠다.

- `parallel_scan` 이 1단계 자식이 프로세스보다 적으면 **더 깊이 펼쳐 병렬 단위를 확보**
  (procs×4 목표). 펼친 부모는 직속 파일만(shallow), 그 자식들은 재귀(deep) → 누락/중복
  없음. per_top(1단계 자식별 드릴다운)은 원래 top 라벨로 합산해 **그대로 유지**.
- 합계가 os.walk 와 정확히 일치함을 자식 2개 트리(→34 단위로 펼침)로 테스트 고정.
- pscan CLI(`-P/-T`)도 코드 변경 없이 자동으로 혜택.

### 정직성 메모

- 분할 '메커니즘'과 정합성은 구현·테스트했다. user 같은 영역을 13배로 끌어올리는 '실 효과'는
  **실 NAS 의 before/after 측정**으로 확인해야 한다(로컬엔 지연이 없어 분할 효과가 안 보인다).

---

## [1.51.0] - 2026-06-14

### 변경됨 (Changed) — bench_walk `--only` 다중 선택(측정 시간 단축)

`--only` 가 쉼표로 **여러 전략**을 받게 했다. 6전략 풀세트(×60초=6분) 대신 핵심 2개만
짧게 비교할 수 있다 — 예: `--only "serial,procs8 x thr8" --secs 15` → 30초.

- 실측 교훈: NAS 메타데이터 속도는 **영역·캐시상태마다 극과 극**(esko-prd serial 11,209
  files/s 빠름 → 병렬 손해 0.25x / user·전체 cold ~1,300~1,600 느림 → 병렬 필요). 빠른
  영역은 시간을 안 잡아먹으니 진단은 **느린 cold 영역**에서 serial vs procs×thr 만 빠르게.

---

## [1.50.0] - 2026-06-14

### 변경됨 (Changed) — pscan CLI 에 `--threads`(2단 병렬) 노출

`parallel_scan()` 은 `threads_per_proc`(프로세스×스레드 2단 병렬)를 이미 지원했지만 **CLI 가
이를 전달하지 않아**, `pscan` 명령은 프로세스×스레드1(=GIL 회피만, 왕복 지연 은닉 없음)밖에
못 돌렸다. 즉 bench_walk 가 고른 승자(procs8×thr8)를 명령줄에서 **실제로 실행할 수 없었다**.

- `pscan` 에 **`--threads/-T`**(기본 1) 추가 → `parallel_scan(threads_per_proc=...)` 로 전달.
  실행 예: `python3 -m isilon_usage pscan /mnt/hadoop -P 8 -T 8` (동시 64 stat).
- `--compare` 도 `--threads` 를 존중(프로세스 1·2·4·8 × 지정 스레드로 확장성 비교).
- 헤더에 `프로세스 N × 스레드 M (동시 N*M)` 표기.
- 정직성: 고지연 NAS 에서만 `-T>1` 이 이득(왕복 지연 은닉). 로컬 빠른 저장소는 `-T 1` 유지.

### 기타

- CLAUDE.md: "모든 답변은 한글로" 대화 원칙 추가(사용자 요청).

---

## [1.49.0] - 2026-06-14

### 변경됨 (Changed) — bench_walk: 실 NAS 에서 '몇 분'만에 측정(시간상자·ETA)

기존 `--path` 는 6개 전략을 각각 **전체 풀스캔** 해서 6PB 에선 전략당 수 주(週) × 6 = 비현실적
이었다. 처리량(files/s)은 '비율'이라 전체를 다 걸을 필요가 없다 — 대표 표본을 짧게 재고 외삽한다.

- **`--secs N`** — 각 전략을 N초만 돌려 files/s 측정(시간상자). 실 NAS 30~120초 권장.
  거대 평면 디렉터리도 8192파일마다 마감 점검해 제때 멈춘다(프로세스 전략은 자식에 마감
  시각 상속).
- **`--total <전체파일추정>`** — 주면 전략별 **풀스캔 ETA** 로 환산해 출력
  (예: `전체 6,000,000,000 예상 8h12m`). 추정치는 아이실론 FSA/쿼터나 표본으로.
- **`--only <문자열>`** — 그 문자열이 든 전략만 실행(절대 files/s·ETA 를 cold 한 별도
  디렉터리에서 따로 잴 때).
- 캐시 주의 안내 강화: 같은 경로 반복은 '뒤 전략'이 캐시로 유리(배속 부풀려짐) → 비교는
  `--secs`, 절대 수치는 `--only` 로 cold 표본에서.
- 기본 동작 불변(`--secs` 미지정=기존 풀스캔). 합성 모드도 동일 플래그 지원.

---

## [1.48.0] - 2026-06-14

### 추가됨 (Added) — 스캔 회차(#)별 소요 시간·결과 이력

- **스캔 이력 카드(대시보드)** — 기존 개요 표는 ‘루트별 최신 스캔’만 보여줘서 같은 경로를
  여러 번 돌리면 과거 회차(#1·#2…)가 가려졌다. 이제 그 아래 **‘📋 스캔 이력 — 회차(#)별 소요
  시간·결과’** 표에 **모든 회차**를 최신순으로: **회차(#번호) · 루트 · 시작 시각 · 소요 시간
  (진행 중이면 실시간) · 조사 용량 · 파일 수 · 상태 · ‘결과 보기’**. ‘결과 보기’를 누르면 그
  회차의 집계 화면으로 이동한다. 별도 백엔드 변경 없이 기존 `/api/scans`(회차별
  started_at/finished_at)로 렌더 — 소요 시간은 완료=`finished−started`, 진행 중=`now−started`.

---

## [1.47.0] - 2026-06-14

### 변경됨 (Changed) — 스레드 스캐너 직렬 구간 축소(엔진 업그레이드 2단계)

스레드 스캐너의 천장이던 **단일 `_dlock` 임계구역 + 디렉터리마다 DB 커밋**을 줄였다.
진단상 stat(느린 I/O)은 이미 락 밖이지만, stat 이후의 **파일당 파이썬 작업(나이/접근나이/
소유자/확장자 집계 + Top-N 힙)과 per-dir `commit()`(fsync)** 이 전부 한 락 안에서 직렬화돼
워커들이 락에 줄 서느라 NAS 로 stat 을 충분히 못 쏘던 문제.

- **파일당 집계를 스레드 로컬로** — 청크의 나이/확장자/소유자/Top-N 을 `_dlock` **밖**에서
  로컬에 모으고, 락 안에서는 **짧게 병합만** 한다(하드링크 dedup 은 공유라 락 안에서 판정 —
  보통 nlink=1 이라 그 경로를 안 탐). 워커가 곧바로 다음 stat 을 발사 → 동시 in-flight stat ↑.
- **커밋 배칭** — 디렉터리마다 하던 `commit()`(fsync)을 없애고 진행 flush(최대 0.4s 스로틀)에서
  한 번에 커밋한다. 같은 커넥션이라 미커밋 쓰기도 다른 워커 claim 에 보여 정합성 유지, 종료 시
  마지막 커밋이 꼬리를 비우고, kill -9 면 claimed→pending 으로 되돌아 재스캔되어 안전.
- **측정(지연 0.3ms/stat 주입, 합성 10,368파일 A/B):** 워커 8 = 8,556→**9,980 files/s(+17%)**,
  워커 16 = 7,099→**9,870 files/s(+39%)**. 구버전은 워커 8→16 에서 **오히려 느려졌는데**(락 경합)
  신버전은 유지된다. 파일 수는 동일(정합성 보존). 하드링크가 여러 디렉터리에 흩어져도 워커
  1/8 결과가 동일함을 테스트로 고정.

### 정직성 메모

- 위 수치는 로컬 FS에 지연을 주입한 시뮬레이션이다(메커니즘 증명용 +17~39%). **실 NAS 의
  before/after 스캔 시간**으로 확인이 필요하다. 이번 변경은 '동시성을 더 높여도 천장에 빨리
  안 부딪히게' 하는 구조 개선이고, 큰 도약(프로세스×스레드 24배)은 pscan 2단 병렬(1.46.0) 쪽이다.

---

## [1.46.0] - 2026-06-14

### 추가됨 (Added) — 스캔 엔진 업그레이드 1단계: 측정 도구 + 2단 병렬(옵트인)

스캔 병목 진단(GIL + 단일 `_dlock` + 단일 DB 커넥션 직렬화)을 토대로, "더 많은 파일을
더 빨리 찾기 = 동시 메타데이터 요청 수로 NAS 왕복 지연 은닉"이라는 처방을 **측정 우선**으로
착수한다(측정 결과 DB·저장형식 교체는 무의미, 동시성이 핵심).

- **`tools/bench_walk.py` — 실 NAS 측정 벤치(읽기 전용)** — 같은 트리를 serial / threads /
  procs / **procs×threads(2단)** 전략으로 훑어 files/s 를 비교한다. `--path <NAS경로>` 로
  **실제 클러스터에서** 스케일링 곡선을 측정하고(아무것도 쓰지 않음), 경로 없이 실행하면
  지연 주입(`--lat`)으로 메커니즘을 모사한다. **로컬 빠른 저장소에선 스레드가 손해,
  고지연 NAS 영역에선 프로세스×스레드가 최고**임을 같은 코드로 확인할 수 있다.
- **pscan 프로세스당 stat 스레드(2단 병렬)** — `parallel_scan(..., threads_per_proc=N)` 로
  각 프로세스가 자기 단위를 다시 N 스레드로 훑어 **NFS 왕복 지연을 은닉**한다(프로세스=GIL
  회피 × 스레드=지연 은닉 → 동시 in-flight stat 을 processes×N 으로). 임계구역은 카운터/큐뿐,
  프로세스 간 공유 상태 없음(기존 pscan 정합성 그대로). 합계가 직렬과 정확히 일치함을 테스트로
  고정. 엣지 설정 **`pscan_threads`(기본 1=동작 변화 없음, 옵트인)** 로 켠다.

### 정직성 메모

- 벤치의 시뮬레이션(로컬+지연주입) 수치는 **메커니즘 증명용**이다. **실수치는 반드시
  `--path` 로 실 NAS 에서 측정**해야 한다(지연 분포·서버측 동시성 한계·속성 캐시가 다름).
- 6 PB 스케일의 진짜 답은 여전히 **아이실론 네이티브(SmartQuotas 어카운팅 쿼터 · FSA)** —
  파일을 걷지 않고 클러스터 메타데이터로 조회(실 클러스터 없으면 미검증).

---

## [1.45.0] - 2026-06-14

### 추가됨 (Added) — 인프라 체크(서버 Ping 이력) · 스캔 시작 버튼 · 설정 탭 정리

- **인프라 체크 (서버 Ping 이력, 1년 누적)** — 네트워크 모니터링 탭을 재설계. 매니저(HQ)에서
  각 노드로의 지연(ms)을 **백그라운드로 1분마다 측정해 `ping_history.db` 에 누적**(366일 보존,
  1시간마다 오래된 표본 정리). **지역(region)별로 묶인 서버별 카드**에 시간축 산점/선 차트를
  그리고, 점은 **평소(중앙값) 대비 편차로 색칠**(🔵 정상 · 🟡 +20% 이상 · 🔴 +50% 이상),
  **중앙값 점선**·y축 최대 라벨·시간 범위를 표시한다. **기간 버튼 1일/7일/30일/90일/365일 +
  새로고침 + 범례.** 다운샘플(노드별 ~240점)로 가볍게. 새 엔드포인트
  `GET /api/portal/ping-history?range=<초>` (노드별 series·중앙값 반환).
- **노드 표 ‘▶ 스캔 시작’ 버튼** — 글로벌 대시보드 노드 표에서 **온라인이지만 스캔 중이 아닌**
  노드(이전 스캔 기록 있음)의 ‘온라인’ 배지 옆에 버튼을 두어, **마지막 스캔과 동일한 경로로**
  새 스캔을 시작한다. 새 엔드포인트 `POST /api/portal/node-scan {id}` (로그인 필요) → 엣지
  `POST /api/scan/start`. 엣지에 로그인 비밀번호가 걸려 있으면(op_required) 포탈엔 그 토큰이 없어
  시작할 수 없으므로 **그 사유를 친절히 표시**(엔진/백엔드는 엣지 기본 설정을 따름).

### 변경됨 (Changed)

- **‘노드 설정’ 페이지를 용도별 하위 탭으로 정리** — 한 페이지에 모든 카드를 나열하던 것을
  **① 🚀 설치·구성 → ② 🔄 업그레이드 → ③ 🗂 노드 관리 → ④ 🔒 보안·감사** 4개 탭으로 분리.
  **설치를 맨 앞**에 두어 노드 도입 흐름(설치→업그레이드→관리→보안)이 한눈에 보이게 했다.

### 수정됨 (Fixed)

- **네트워크 모니터링 실시간 표가 비어 보이던 버그** — `/api/portal/ping` 응답의 키가
  `results` 인데 프런트가 `nodes` 를 읽어 표가 채워지지 않던 문제를 고쳤다.

---

## [1.44.0] - 2026-06-14

### 추가됨 (Added) — 네트워크 모니터링 메뉴 (HQ→노드 핑)

- **네트워크 모니터링 탭** — 포탈 상단 '글로벌 대시보드' 옆에 추가. 등록된 모든 노드로의
  **HQ→노드 왕복 지연(ping)을 5초마다 자동 측정**해 **UP/DOWN · 지연(ms) · 최소/최대 · 손실률 ·
  추이(스파크라인)** 를 표로 보여준다. 상단에 UP/DOWN 개수·평균 지연 요약. 기존
  `/api/portal/ping` 을 보기 활성 시에만 주기 폴링으로 재사용(추가 부하 최소).

---

## [1.43.2] - 2026-06-14

### 수정됨 (Fixed) — 업그레이드가 실제로 적용 안 되던 문제

- **원격 업그레이드가 엉뚱한 디렉터리에 풀려 적용 안 되던 버그 수정** — 업그레이드 스크립트가
  새 코드를 고정 `EDGE_DIR`(기본 `/opt/isilon_edge`)에 풀었는데, 서비스가 다른 디렉터리에서
  코드를 로드하면 재시작해도 옛 코드가 떠 **rc=0(성공)인데 버전이 안 바뀌었다.** 이제 **systemd
  에서 서비스의 실제 코드 디렉터리(WorkingDirectory)를 자동 감지**해 거기에 풀고, **옛→새
  버전**을 출력해 실제 적용 여부를 확인할 수 있게 했다.

### 변경됨 (Changed)

- **전 노드 업그레이드 결과를 종료코드 대신 성공/실패 + 상세 사유로** — `rc=0`/`rc=255` 대신
  **"성공"/"실패: <사유>"** 로 보여주고, SSH 실패 시 **SSH 출력(연결 거부·인증 실패 등)을 그대로**
  붙여 원인을 자세히 표시한다.

---

## [1.43.1] - 2026-06-14

### 변경됨 (Changed)

- **전 노드 SSH 업그레이드 실패 사유 표시** — 실패 노드에 `rc=255` 같은 코드만 보이던 것을,
  **SSH 출력의 마지막 줄**(예: "Permission denied", "Connection refused")이나 "SSH 연결/인증
  실패(rc 255)" 로 보여줘 원인을 바로 알 수 있게 했다.

---

## [1.43.0] - 2026-06-14

### 추가됨 (Added) — 전 노드 SSH 일괄 업그레이드(부트스트랩) + 친절한 푸시 결과

- **전 노드 SSH 일괄 업그레이드** — 포탈 '노드 설정 → 🔁 자동 업그레이드'에 SSH 옵션 추가.
  엣지가 구버전(<1.41.0, `/api/upgrade` 없음)이라 푸시를 못 받을 때, 포탈이 각 엣지에 **SSH로
  접속해 업그레이드 스크립트를 실행**(병렬)해 한 번에 올린다(엣지가 HQ에서 새 코드를 받아
  교체·재시작). `POST /api/portal/upgrade-all/ssh`. 공용 계정/비번 기준, 비번은 1회성.
- **푸시 결과 메시지 개선** — '전 노드 지금 업그레이드' 실패 사유를 코드 그대로(`HTTP 404`)가
  아니라 **사람이 읽는 안내**로: 404 → "구버전 엣지(<1.41.0) 푸시 미지원 — SSH 부트스트랩 필요",
  403 → "엣지에 api_token 설정 필요".

> 배경: 1.41.0 이전 엣지에는 푸시 수신용 `/api/upgrade` 가 없어 첫 업그레이드(예: 1.36→)는
> 푸시로 불가하다. 이 SSH 일괄 업그레이드로 부트스트랩하면, 이후부터는 원클릭 푸시가 된다.

---

## [1.42.2] - 2026-06-14

### 수정됨 (Fixed)

- **포탈 복제 중 `portal_nodes.json.tmp` 저장 레이스 수정** — 여러 노드를 병렬로 복제할 때
  노드 레지스트리 저장이 **같은 임시파일명**을 공유해, 한 스레드가 원자 교체하면 다른 스레드의
  `os.replace` 가 `[Errno 2] No such file or directory` 로 실패했다. **스레드별 고유 임시파일** +
  **상태 변경/저장을 락으로 직렬화** + 스냅샷 저장으로 해결(병렬 600회 저장 무오류 검증).

### 변경됨 (Changed)

- **포탈 용량 색상 임계값** — 노드 표 사용률 막대 색을 **70%↑ 노랑 · 80%↑ 주황 · 85%↑ 빨강**
  (이하 초록)으로 조정(기존 75%↑ 주황·90%↑ 빨강).

---

## [1.42.1] - 2026-06-14

### 변경됨 (Changed)

- **포탈 노드 표 '상태' 정렬을 스캔 경과시간 기준으로** — '상태' 머리글을 클릭하면 진행 중
  스캔을 **오래 스캔한 순**으로 정렬한다(동률·비스캔은 스캔중 > 온라인 > 오프라인 분류로 보조
  정렬). 머리글에 안내 툴팁 추가.

---

## [1.42.0] - 2026-06-14

### 추가됨 (Added) — 아이실론 활성 알람(이벤트) 점검

- **아이실론 알람 점검** — OneFS 미해결 이벤트(`eventgroup-occurrences`)를 개수만이 아니라
  **심각도(긴급/심각/경고/정보)·메시지·시각·발생횟수**까지 가져와 대시보드 '스토리지 어레이
  상태' 카드에 **활성 알람 목록**으로 보여준다(접어두기 + 심각도별 집계 요약).
- **3단계 상태 판정** — 심각/긴급 알람이 있으면 **위험**, 경고/미해결이 있으면 **주의**, 없으면
  **정상**(기존 2단계에서 확장). OneFS 버전별 응답 필드 차이를 방어적으로 파싱하며, 파싱
  로직은 순수 함수로 분리해 단위 테스트한다.

---

## [1.41.1] - 2026-06-13

### 수정됨 (Fixed) — 전체 소스 리뷰 반영

- **pscan 스캔 재개(resume) 오류 수정** — `backend=pscan` 스캔을 재개하면 스레드 스캐너가
  pscan 의 얕은 per-run DB 위에서 돌아 잘못된 결과를 냈다. pscan 은 부분 재개가 없으므로
  **전체 재스캔**으로 라우팅하고 per-run DB 를 새로 기록하게 고쳤다.
- **자동 업그레이드가 진행 중 스캔을 끊던 문제 완화** — 감시 폴더 자동 업그레이드가 실행 중
  스캔을 강제 종료(고아화)했다. 이제 **진행 중 스캔/동기화가 있으면 업그레이드를 미룬다**
  (스캐너는 실행 중 스캔, 포탈은 동기화 중일 때 보류).
- **업그레이드 아카이브 크기 상한** — 손상·거대 아카이브(zip/tar 폭탄)의 메모리 폭주를 막기
  위해 압축 해제 총량(200MB)·멤버 수 상한을 둔다.

### 문서 (Docs)

- `SECURITY.md` 갱신: 자동 업그레이드 보안(감시 폴더 쓰기권한=RCE 신뢰 요구·포탈→엣지 푸시
  MITM), 로그인 세션/무차별 대입 속도 제한의 한계, 보기(GET) 무인증 공개 범위 명시.
  스테일한 "10초 유효" 문구를 세션 30분으로 정정. `portal_settings.json`·`audit.log` 민감
  파일 추가.

---

## [1.41.0] - 2026-06-13

### 추가됨 (Added) — 감시 폴더 자동 업그레이드 + DC(엣지) 일괄 업그레이드

- **감시 폴더 자동 업그레이드** — 설정에 **감시 폴더**(`upgrade_watch_dir`)를 지정하면, 그
  폴더에 현재보다 새 버전 `isilon_usage-*.tar.gz/.zip` 이 생길 때 **자동으로 패키지를 교체하고
  프로세스를 재시작**(re-exec)한다. 스캐너·포탈 공통, **옵트인**(비우면 끔). 더 새 버전만
  적용하고 **기존 코드는 백업**한다. 표준 라이브러리만 사용(공용 `upgrade.py`).
- **DC(엣지) 일괄 업그레이드** — 포탈이 자가 업그레이드되면(디스크의 새 코드 = 새
  agent-bundle) **등록된 모든 엣지에 새 코드 번들을 푸시**해 함께 올린다(엣지 `POST
  /api/upgrade`, 엣지 `api_token` 인증 — SSH·자격증명 불필요). 포탈 '노드 설정 → 🔁 자동
  업그레이드' 에서 감시 폴더 지정 + **전 노드 지금 업그레이드** 수동 실행도 가능.
- 안전장치: 옵트인, 아카이브·버전 검증, 더 새 버전만 적용, 경로 탈출 방지, 기존 코드 백업,
  업그레이드 이벤트 감사 로그 기록. 엣지 푸시 수신은 `api_token` 미설정 시 거부한다.

---

## [1.40.0] - 2026-06-13

### 추가됨 (Added) — 멀티프로세스(pscan) 엔진을 웹·예약 스캔에 연결

- **빠른 용량 스캔 엔진(pscan)** — 새 스캔 시작에서 **엔진: threads(상세 트리) / pscan(멀티
  프로세스)** 을 고를 수 있다. pscan 은 GIL 을 우회해(프로세스 분리) 스레드 대비 처리량이
  크게 오른다(측정 4워커 5.48배). 그동안 CLI PoC 로만 있던 멀티프로세스 스캐너를 웹/예약
  스캔에서 바로 쓸 수 있게 했다.
- pscan 결과(루트 + 1단계 자식)를 표준 per-run DB 로 기록해 **대시보드의 용량 개요·NAS별
  현황·1단계 드릴다운**에 그대로 표시된다(매니저 요약도 갱신).
- 정직한 한계: pscan 은 **깊은 디렉터리 트리를 만들지 않는다**(루트+1단계까지만). 깊은
  드릴다운이 필요하면 threads 엔진을 쓴다. pscan 스캔은 실행 중 '중지'가 즉시 반영되지
  않는다(프로세스 풀이 끝까지 실행). 설정 `default_engine` 으로 기본 엔진을 지정할 수 있다.

---

## [1.39.0] - 2026-06-13

### 추가됨 (Added) — 로그인 감사 로그 + 무차별 대입 방지

- **무차별 대입 방지** — 로그인 비밀번호를 연속 5회 틀리면 30초간 잠가 자동 대입 공격을
  늦춘다(스캐너·포탈 공통, 공용 `AuthGuard`). 잠긴 동안엔 올바른 비밀번호도 막힌다.
- **감사 로그** — 로그인 성공/실패, 설정 변경, 스캔 시작/중지, 노드 등록·원격 구성·업그레이드
  등 변경 작업을 `<data-dir>/audit.log`(JSONL)에 **출처 IP·시각**과 함께 남긴다. 설정의
  **🧾 감사 로그 보기**(로그인 필요)로 최근 300건을 조회한다(포탈도 동일). 조회 API 는
  로그인 토큰을 요구하고, 출력은 XSS 방지를 위해 이스케이프한다.
- 스캐너 인증을 공용 `AuthGuard` 로 통일(기존 인라인 토큰 로직 제거 — 중복 해소). 세션·잠금
  정책을 스캐너·포탈이 동일하게 따른다.

---

## [1.38.0] - 2026-06-13

### 추가됨 (Added) — 원격 엣지 버전 업그레이드 (포탈)

- **원격 버전 업그레이드** — 포탈 '노드 설정 → 🔄 원격 버전 업그레이드' 에서 엣지를 HQ 의
  최신 코드로 올린다. HQ 포탈에서 최신 코드(agent-bundle)를 받아 엣지 코드 디렉터리에
  덮어쓰고 서비스를 재시작한다(스캔 DB·설정은 data-dir 라 보존). 두 가지 방식:
  - **A) 업그레이드 스크립트 생성**(`POST /api/portal/upgrade`) — 엣지에서 복붙 1회 실행(무의존·안전).
  - **B) SSH 자동 업그레이드**(`POST /api/portal/upgrade/ssh`) — 포탈이 SSH 로 접속해 실행(옵션, 신뢰망·키 권장).
- 업그레이드 시 **HQ 버전과 노드 버전**을 함께 표시. 생성 스크립트의 HQ 주소는 포탈 접속
  주소(`location.origin`)로 자동 채운다.
- provision 의 SSH 실행부를 공용 `_ssh_run` 으로 정리해 provision/upgrade 가 함께 쓴다(중복 제거).
- 업그레이드 작업도 **로그인(인증) 필요**(v1.37.0 로그인 게이트 적용).

---

## [1.37.0] - 2026-06-13

### 추가됨 (Added) — 사용자 인증(로그인): 읽기 전용 기본 + 로그인 후 전체 권한

- **로그인 기반 접근 제어(스캐너·포탈 공통)** — 비밀번호를 설정하면 접속 시 **읽기 전용**이
  되어 조회만 가능하고, 우상단 **🔑 로그인** 후에야 스캔 시작·설정 변경·노드 등록·원격
  구성 등 모든 변경 작업을 할 수 있다. 로그인 세션은 기본 30분(이후 자동 로그아웃).
  보기(GET)는 로그인 없이 자유롭다.
- **포탈 인증 신설** — 그동안 인증이 전혀 없던 글로벌 포탈에도 동일 로그인 모델을 적용했다.
  노드 등록/삭제/동기화·원격 자동 구성(provision/SSH) 등 모든 변경 작업이 로그인 토큰을
  요구한다(비밀번호 미설정 시에는 기존처럼 공개). 포탈 비밀번호는 `portal_settings.json` 에 저장.
- **비밀번호 암호화 저장 옵션** — 기본은 설정 파일에 **평문** 저장이며, 설정의 **"비밀번호
  암호화 저장"** 체크 시 **PBKDF2-SHA256 해시**로 저장한다(파일에 평문을 남기지 않음·복구
  불가, 검증만 가능). 암호화 저장 시 평문 복구용 `info.MD` 는 기록하지 않는다.
- 공용 인증 모듈 `auth.py`(해시/검증 + 세션 토큰 `AuthGuard`) 추가 — 스캐너·포탈이 함께
  쓴다. 표준 라이브러리만 사용(무의존).

### 보안 (Security)

- 위 포탈 인증으로 **이전에 지적된 '포탈 무인증 노출'(원격 SSH·노드 변경 등)** 위험을 막는다.
  단, 비밀번호 설정 전(부트스트랩 구간)과 보기 전용 GET 은 공개이므로, 외부 노출 시 기동
  직후 비밀번호를 설정하고 신뢰망/리버스 프록시 뒤에 두기를 권장한다.

---

## [1.36.0] - 2026-06-13

### 추가됨 (Added) — NAS별 현황 (한 서버에서 여러 NAS 점검)

- **NAS별 현황 카드** — 대시보드 '관리 개요' 아래에 **NAS 중심** 표를 추가했다. 허용
  경로(mount_bases)로 등록한 NAS 와 실제 스캔된 루트를 한 줄씩 모아 디스크 사용/사용%/
  파일 수/마지막 스캔/예약/상태를 보여준다(★=등록 NAS). 아직 스캔 안 한 NAS 는 '미점검'
  으로 뜨고 **"지금 스캔"** 으로 바로 시작한다. 별도 API 없이 기존 `/api/scans`
  데이터(roots+mount_bases)와 설정의 예약(schedules)을 클라이언트에서 조인해 그리므로
  추가 부하가 없다.
- **예약 시차 배치** — `POST /api/schedules/stagger` + "예약 시차 배치" 버튼. 한 서버에서
  여러 NAS 를 짧은 주기로 예약 스캔할 때 동시에 시작하면 GIL/단일 DB 직렬화로 경합만
  커지므로, **간격형(분/시간) 예약들의 다음 실행 시각을 주기 안에서 고르게 분산**한다.
  달력형(일/주/월)의 지정 시각(at)은 같은 날 중복 실행을 막기 위해 보존한다.

---

## [1.35.0] - 2026-06-13

### 변경됨 (Changed) — 모든 설정·데이터를 /data/isilon_usage 에 고정(업그레이드 보존)

- **기본 data-dir 를 절대경로 `/data/isilon_usage` 로 통일** — 스캐너(`serve`/`run`)와
  포탈(`portal`) 모두 같은 기본 위치를 쓴다(이전: 상대경로 `isilon_data` /
  `isilon_portal_data`). 코드 디렉터리(`/opt/...`)와 분리된 절대경로라 **코드를
  업그레이드해도 설정·DB 가 사라지지 않는다.** 파일명이 겹치지 않아(스캐너
  `settings.json`·`manager.db`·`scans/`·`metrics.db` / 포탈 `portal_nodes.json`·
  `replicas/`) 한 디렉터리를 공유해도 안전하다. `--data-dir` 로 언제든 변경 가능.
- **레거시 설정 자동 이관(비파괴)** — 기본 data-dir 로 처음 기동할 때, 이전 위치
  (`/var/lib/isilon_usage`·`/var/lib/isilon_portal`·`isilon_data`·`isilon_portal_data`)의
  `settings.json`·`portal_nodes.json` 을 새 위치로 1회 복사한다(원본은 남김). 업그레이드
  후에도 기존 설정·노드 목록이 유지된다.
- systemd 유닛 2개의 `--data-dir` 를 `/data/isilon_usage` 로 갱신(StateDirectory 제거,
  앱이 디렉터리 자동 생성). 원격 자동 구성 스크립트의 기본 data-dir 도 동일하게 변경.

---

## [1.34.1] - 2026-06-13

### 수정됨 (Fixed)

- **포탈 systemd 유닛 기동 실패(`status=200/CHDIR`) 수정** — `isilon_usage_portal.service`
  가 존재하지 않을 수 있는 `/opt/isilon_portal` 을 `WorkingDirectory` 기본값으로 달고
  있어, 간단 설치(코드가 `/opt/isilon_usage` 에만 있는 경우) 사용자가 유닛을 그대로
  `enable` 하면 systemd 가 그 디렉터리로 `chdir` 하지 못해 `EXIT_CHDIR` 로 크래시-재시작
  루프에 빠졌다. 기본 `WorkingDirectory` 를 스캐너와 동일한 **`/opt/isilon_usage`** 로
  통일해 간단 설치에서 바로 기동되게 했다. 업그레이드 격리(포탈만 따로 교체)가 필요하면
  `/opt/isilon_portal` 을 만들어 패키지를 복사하고 이 값을 바꾸도록 유닛 주석과
  `docs/SERVICE.md` 에 명시. (포탈 데이터는 여전히 `--data-dir /var/lib/isilon_portal`
  로 분리되며, 설정·노드 목록은 그 안의 `portal_nodes.json` 에 저장된다.)

---

## [1.34.0] - 2026-06-13

### 추가됨 (Added) — 포탈에서 원격 엣지 자동 구성

- **🚀 원격 자동 구성(A·기본·안전)** — 포탈 '노드 설정'에서 원격 IP/포트/경로만
  넣으면 **엣지에서 복붙 실행할 설치 스크립트를 생성**하고 **노드를 자동 등록**한다.
  스크립트는 HQ 포탈에서 코드를 받아(`/api/portal/agent-bundle`, 폐쇄망도 인터넷
  없이) `serve` 를 systemd/nohup 으로 기동(api_token·mount-base 포함). 비밀번호를
  다루지 않아 안전. `POST /api/portal/provision`.
- **에이전트 번들** — `GET /api/portal/agent-bundle` 로 포탈이 실행 중인 패키지를
  tar.gz 로 내려준다(엣지 부트스트랩용).
- **SSH 자동 푸시(B·옵션)** — IP/계정/비번(또는 키)으로 포탈이 SSH 접속해 원격
  설치까지 실행(`POST /api/portal/provision/ssh`). `ssh`/`sshpass` 가 있을 때만,
  없으면 생성 스크립트를 동봉해 우아하게 실패. 비밀번호는 보관하지 않고 1회만 사용.
  무의존 원칙상 **옵션**이며 신뢰망·키 인증 권장.
- 스캐너 `serve`/`run` 에 **`--api-token`** 플래그 — 신규 설치 시 복제 토큰을 한
  번에 시드(원격 자동 구성의 무인 설치를 위함).
- 포탈 용량 '—' 셀에 **툴팁**(엣지가 fs 전체용량 미보고 안내) 추가.

---

## [1.33.0] - 2026-06-13

### 추가됨 (Added) — 글로벌 포탈(관제센터) 강화

- **제목 변경**: 포탈을 **"다빈치 글로벌 아이실론 관제센터"** 로 명명.
- **노드별 용량 사용률** — `용량(사용률)` 열에 **사용/전체 막대 + %** 와 함께
  `사용 / 전체` 용량(2가지)을 표기. 상단에 **전 DC 전체 스토리지 용량 막대**
  (사용/전체/여유 · %) 추가. (엣지가 보고한 fs_used/fs_total 기반, 같은 FS 중복 제외.)
- **스캔 시작 시각 / 진행 시간** — 진행 중 노드 상태칸에 `⏱ 경과 · 시작 시각`,
  스토리지 펼침 행에도 시작/진행 시간 표시(엣지 시계 기준 경과라 시차 영향 없음).
- **노드 Ping 체크** — `📡 Ping 체크` 버튼으로 **HQ→각 노드 왕복 응답시간(ms)** 을
  병렬 측정(평균·응답 수 요약, 지연별 색상). `GET /api/portal/ping`.
  (포탈이 능동 측정 가능한 HQ↔노드 지연이며, 엣지끼리의 DC↔DC 메시 핑은 아님.)
- **디자인 보강** — 상단 **실시간 시계**, 노드 **버전 배지(구버전 ⬆ 강조)**,
  **마지막 동기화 신선도 색상**(초록/노랑/빨강), 용량 막대 색상(70/85% 임계).

### 변경됨 (Changed)

- 포탈 노드 표: `버전` 열을 노드 이름 옆 배지로 옮기고 그 자리에 `용량(사용률)` 열 추가.
- `portal.overview` 의 totals/노드에 `fs_total_bytes·fs_used_bytes·fs_used_pct·running` 추가.

---

## [1.32.1] - 2026-06-12

### 개선됨 (Improved) — 설정 화면 줄 맞춤(가독성)

- 설정 폼의 입력칸이 라벨 줄 수(1~2줄)에 따라 들쭉날쭉하던 문제를 수정. 각 라벨
  텍스트에 **고정 높이(min-height)** 를 줘서 같은 행의 입력칸들이 **같은 높이로
  정렬**되도록 함(`.setlbl`).
- 가장 길던 라벨 2개(깊이 접기 / 탐색 최대 깊이)를 2줄에 맞게 **축약**하고, 잘린
  상세 설명은 **마우스 오버 툴팁(title)** 으로 옮겨 정보 손실 없이 정렬을 맞춤.

---

## [1.32.0] - 2026-06-12

### 추가됨 (Added) — 마지막 접근시각(atime) 분석

- **분석 리포트에 '🧊 마지막 접근 나이(atime)' 분포** 추가 — 기존 수정시각(mtime)
  '파일 나이'와 나란히, 마지막 **접근** 기준 30일/90일/1년/2년/5년+ 용량 분포.
  "오래 접근 안 한 콜드 데이터"(아카이브·계층화 후보) 식별용.
- **최대 파일 Top 목록에 '접근(atime)' 열** 추가(수정시각 옆).
- **atime 신뢰도 경고** — 대상 마운트의 atime 정책을 자동 판정(`systune.atime_policy`).
  `noatime`(갱신 안 함)이면 "이 분포는 신뢰 불가" 경고, `relatime`이면 "하루 단위
  거친 갱신(콜드 판별엔 사용 가능)" 안내를 대시보드·CLI 양쪽에 표시.
- **추가 I/O 0** — atime은 스캐너가 이미 호출하는 `stat()` 결과에 들어 있어, 수집에
  syscall이 한 번도 더 늘지 않는다(병목 GIL/직렬화와 무관).

### 변경됨 (Changed)

- **DB 스키마 8 → 9**: `top_files` 에 `atime` 컬럼, `scan_stats` 에 `atime_age` 종류
  추가. 기존 DB는 누락 컬럼을 자동 보강(하위호환, 재스캔 불필요·과거 행은 atime 공란).

---

## [1.31.5] - 2026-06-12

### 보안 (Security) — 공개 배포 전 보강

- **탐색(`/api/browse`) 경로 제한** — `mount_bases` 가 설정돼 있으면 그 하위
  경로만 폴더 나열을 허용한다. 이전에는 임의 경로(`/etc` 등)의 디렉터리 구조를
  나열할 수 있었다. 마운트 루트에서 '위로' 가도 허용 경로 밖으로는 못 나간다.
  (`mount_bases` 가 비어 있으면 기존 동작 유지 = 스캔 허용 정책과 동일.)
- **비보안 기동 경고** — `serve` 가 비루프백 주소(예: 기본 `0.0.0.0`)에 **작업
  보호 비밀번호 없이** 뜨면 stderr 로 경고를 출력한다(네트워크의 누구나 스캔
  시작·설정 변경 가능). `--host 127.0.0.1`·작업 보호 비밀번호·`--lock-settings`·
  `--mount-base` 를 권장. 경고만 하고 기동은 막지 않는다(신뢰망 LAN 운영 보호).

---

## [1.31.4] - 2026-06-12

### 추가됨 (Added) — HQ 포탈/스캐너 분리 운영(업그레이드 격리)
- **`packaging/isilon_usage_portal.service`** — 글로벌 포탈(HQ) 전용 systemd 유닛
  (별도 코드 디렉터리·data-dir·포트 8800). 스캐너(serve, 8765)와 분리.
- `docs/SERVICE.md` 에 **'HQ에서 포탈과 스캐너 분리 운영'** 절 — 두 디렉터리
  배치·두 서비스·포탈만 무중단 업그레이드 절차. (포탈/스캐너는 원래 별도 프로세스라,
  코드 디렉터리만 분리하면 포탈 업그레이드가 HQ 디스크 스캔에 전혀 영향 없음.)

---

## [1.31.3] - 2026-06-12

### 추가됨 (Added) — 포탈 노드별 스캔 진행률 + 마지막 스캔 시각
- 포탈 메인 표에 **'마지막 스캔' 열** 추가(그 DC가 마지막으로 스캔/갱신한 시각 =
  데이터 신선도). 노드 행을 펼치면 루트별 **스캔 단계/진행률**: 🔄 탐색 중(N개) /
  📦 집계 N%(processed/total) / ✅ 완료 / ⏸ 일시정지 + 완료·갱신 시각.
- `manager.overall_capacity` 의 roots 에 `discovered_dirs·processed_dirs` 추가,
  `portal.overview` 에 노드별 `last_scan_at` 추가.

---

## [1.31.2] - 2026-06-12

### 변경됨 (Changed) — 글로벌 포탈: 노드별 스캔 상태 일관성
- 포탈 메인 표의 '스캔 중' 배지에 **진행 중 스캔 개수**를 함께 표시(🔄 스캔 중 N).
- **'노드 설정' 관리 표**에도 노드별 **스캔 중/온라인/오프라인** 상태 표시(기존엔
  온라인/오프라인만). `_public_node` 가 `active_scans·scanning·used_bytes` 노출.
- (참고: 노드 등록/수정/삭제/연결테스트/동기화/수집방식(poll·replicate·both)은
  원래부터 모두 '노드 설정' 웹 화면에서 가능 — 추가 작업 불필요.)

---

## [1.31.1] - 2026-06-12

### 추가됨 (Added) — 트러블슈팅에 집계(sizing) 단계 진행률 표시
- 집계(상향식 용량 합산) 단계는 탐색 워커가 없어 트러블슈팅이 **"진행 중 + 0/0"**
  으로만 보이던 맹점 해소. 이제 `phase=sizing` 이면 **집계 진행률(processed_dirs/
  total_dirs)·집계 속도(개/초)** 를 판정과 처리량 카드에 표시한다.
  - 판정: **🟢 집계(용량 합산) 단계 — N%** (하트비트 60s+·속도 0이면 **🟠 집계 정체 의심**).
  - 처리량 카드: 진행바 + "집계 M/T 디렉터리 · R/초", "이 단계는 워커/속도 0이 정상" 안내.
- `diagnose` 결과에 `aggregating·agg_done·agg_total·agg_pct·rate_agg` 추가.

---

## [1.31.0] - 2026-06-12

### 추가됨 (Added) — systemd 서비스 실행 지원
- **`packaging/isilon_usage.service`** — 대시보드(`serve`)를 systemd 서비스로 돌리는
  유닛 템플릿(부팅 자동 시작·실패 시 재시작·세션 끊겨도 유지·journald 로그).
  `After=network-online/remote-fs`, `Restart=on-failure`, `StateDirectory`,
  `LimitNOFILE=65536`, 기본 하드닝, **serve 가 받는 유효 옵션만** 사용.
- **`docs/SERVICE.md`** — 설치/관리/업데이트/보안/자동스캔(run)/비-systemd(nohup) 가이드.
- 릴리스 아카이브에 `packaging/` 포함, README 헤더에 서비스 가이드 링크.

---

## [1.30.4] - 2026-06-12

### 변경됨 (Changed) — 트러블슈팅: 스캔 없을 때 '로그만 보임' 혼란 해소
- 실행 중인 스캔이 없으면 트러블슈팅의 실시간 진단(판정·자원·속도 차트=tsResult)이
  숨겨져 **진단 이력(로그)만 보여** 고장난 것처럼 보이던 문제. → **안내 카드** 추가:
  "스캔이 도는 동안에만 실시간 진단 표시" + **[📈 처리량 추이 보기] 버튼**(스캔 없어도
  과거 시간당 처리량 차트는 볼 수 있음) + 새 스캔 안내. (프론트 전용)

---

## [1.30.3] - 2026-06-12

### 고쳐짐 (Fixed) — 진단 이력이 '정상'으로 도배되던 문제
- 트러블슈팅 **진단 이력이 🟢'정상' 판정으로 가득 차던** 버그 수정. 원인: 이 스캐너는
  GIL 때문에 **일할 때 항상 1코어가 포화(=CPU 빨강)** 인데, 그걸 '문제'로 기록해 정상
  작동 중에도 매번 이력에 쌓였다. → **CPU(GIL 포화)는 정상 작업 상태라 기록 트리거에서
  제외**. 이제 판정이 warn/danger 이거나 **CPU 외 자원(메모리·디스크·NAS)이 'bad'**
  일 때만 이력에 남는다(진짜 문제만). 기존에 쌓인 정상 항목은 '이력 비우기'로 정리.

---

## [1.30.2] - 2026-06-12

### 추가됨 (Added) — 처리량 추이 차트 마우스 오버 툴팁
- 처리량 추이의 세 차트(용량·파일·디렉터리)에 **마우스 오버 툴팁** — 그 지점의
  **시간(버킷 시각)과 처리량**(용량 TB/GB·파일·디렉터리 한 번에)을 보여준다.
  세 차트에 **공통 세로선 + 점**으로 같은 시점을 정렬 표시. (프론트 전용)

---

## [1.30.1] - 2026-06-12

### 변경됨 (Changed) — 자동 새로고침을 '켜두면 계속 유지'
- 트러블슈팅·처리량 추이의 **자동 체크박스를 켜두면**, 새로고침·탭(메뉴) 이동에도
  **풀리지 않고 백그라운드에서 계속 갱신·저장**한다(localStorage 에 설정 저장 →
  다음 로드 때 자동 복원). 기존엔 뷰를 떠나면 자동이 해제됐다.
- 트러블슈팅 자동이 켜져 있으면 다른 메뉴에 있어도 계속 진단해 **진단 이력(History)
  이 끊김 없이 쌓인다**. (프론트 전용 — dashboard.html 만 교체해도 적용)

---

## [1.30.0] - 2026-06-12

### 추가됨 (Added) — "📈 처리량 추이" 메뉴 (버킷별 처리 용량/파일/디렉터리)
- 표본 DB(`metrics.db`)에 **처리 용량(bytes/s)** 도 기록(`rate_samples.rb` 컬럼, 기존
  DB 자동 마이그레이션). 서버 백그라운드 샘플러가 관리 DB의 `scanned_bytes` 델타로 계산.
- **새 메뉴 `📈 처리량 추이`** — **1분 / 5분 / 10분 / 1시간** 버킷으로 *그 시간에 처리한*
  **용량(TB/GB)·파일 수·디렉터리 수**를 추이 차트로 본다(대시보드 닫혀 있어도 24h 누적).
  표본 rate × 시간차를 버킷에 적분해 정확한 버킷별 처리량 산출. 표시 구간 합계도 표시.
- 서버 `GET /api/troubleshoot/throughput?bucket=&max=` (버킷 60~3600초).
  `troubleshoot.throughput_buckets()` + `tests/test_troubleshoot.py` 검증.

---

## [1.29.1] - 2026-06-12

### 변경됨 (Changed) — 트러블슈팅 판정 정확도(디렉터리 진행 정체 감지)
- **"디렉터리 진행 정체"** 판정 추가 — 파일 stat 은 돌아도(files/s>0) **디렉터리
  완료가 안 되면(dirs/s<1) + 대기 일감이 많으면(>1만)** '정상'이 아니라 **🟠 정체**로
  판정한다. 워커 상태에 따라 *롱테일(거대 디렉터리)·굶주림(유휴)·DB 커밋 집중* 으로
  세분. (기존엔 파일 stat 만 돌면 '🟢 정상'으로 떠 정체가 가려졌다 — 진단 이력이 이
  맹점을 드러냈다.)
- '🟢 정상(파일 stat/디렉터리 읽기)' 판정은 **dirs/s 가 실제로 진행될 때만** 찍히게
  가드 추가. 정체는 warn 이라 **진단 이력(History)에도 문제로 자동 기록**된다.
- 처방에 **멀티프로세스(pscan)로 직렬화 제거**를 명시. `tests/test_troubleshoot.py`
  에 판정 테스트 추가.

---

## [1.29.0] - 2026-06-11

### 추가됨 (Added) — 멀티프로세스 병렬 스캔 PoC + 빠른 재작성 설계 문서
- **`pscan`(PoC, `isilon_usage/pscan.py`)** — 진단으로 확인한 병목(GIL + 단일 락
  직렬화)을 **프로세스 분리**로 깨는 멀티프로세스 병렬 스캐너. 루트의 1단계 자식을
  작업 단위로 쪼개 별도 프로세스가 집계(공유 락 없음), 코디네이터가 합산. `du` 와
  동일 계산, `os.walk` 와 정확 일치(프로세스 1/2/4/8 모두, 테스트).
- **멀티노드**: `--node-mount` 로 같은 트리의 여러 노드 마운트를 라운드로빈 분산 →
  아이실론 메타데이터 부하를 전 노드로. CLI `pscan <root> --processes N
  [--node-mount ...] [--compare]`.
- **실측 검증**: 같은 작업에서 스레드(GIL) 1→8 = 0.78×(정체)인데 프로세스 1→8 =
  3.75×(4코어 ≈ 선형), 4워커 기준 **프로세스가 스레드보다 5.48×** 빠름.
- **`docs/DESIGN-fast-rewrite.md`** — 진단·PoC 실측·멀티노드·아이실론 네이티브
  (SmartQuotas/FSA, PAPI 연동)·우선순위·제품화 단계 정리. `tests/test_pscan.py`.

---

## [1.28.0] - 2026-06-11

### 추가됨 (Added) — 처리량 표본 24시간 저장(전용 metrics.db)
- 디렉터리 읽기 속도 표본을 **전용 DB `<data-dir>/metrics.db`** 에 저장한다(기존엔
  브라우저 메모리 10분치 → 새로고침 시 소실). **서버 백그라운드 샘플러**가 10초마다
  관리 DB의 미러된 진행값 델타로 초당 디렉터리/파일 처리량을 계산해 기록하므로,
  **대시보드가 닫혀 있어도 24시간치가 쌓인다.** 5분마다 24h 초과분 자동 정리.
- 차트 표시 구간에 **`1시간 · 24시간`** 추가. 짧은 구간(≤10분)은 브라우저 라이브
  버퍼, 긴 구간(≥1시간)은 metrics.db 표본을 읽어 그린다(많으면 600점으로 다운샘플).
- 서버 `GET /api/troubleshoot/samples?seconds=&max=`. `rate_samples(ts,rd,rf,scan_id)`
  테이블 + 시간 인덱스로 윈도우 조회/정리 효율. `tests/test_troubleshoot.py` 추가.

---

## [1.27.1] - 2026-06-11

### 변경됨 (Changed) — 트러블슈팅 레이아웃 조정
- **처리량·진행 + 속도 차트를 종합 판정 바로 아래 전체폭**으로 이동(가독성↑).
- 차트에 **표시 구간 선택 `5초 / 30초 / 1분 / 10분`** 추가 — 선택 구간에 맞춰
  **자동 폴링 간격도 함께 조절**(5초=1s · 30초=2s · 1분=3s · 10분=5s)해 각 구간이
  의미 있게 보인다. 표본은 최대 10분치 보관, 최근 구간만 그림.
- **자원 병목 점검(½) + 워커 구간 분포(½) 2단 배치**. 자원 표는 측정값·해석을
  한 칸에 합쳐 절반 너비에 맞춤. (프론트 전용 — dashboard.html 만 교체해도 적용)

---

## [1.27.0] - 2026-06-11

### 추가됨 (Added) — 트러블슈팅 진단 이력 + 디렉터리 읽기 속도 차트
- **📜 진단 이력(History)** — 트러블슈팅이 **OK 이외(주의/위험)** 진단을 잡으면
  `<data-dir>/troubleshoot_history.json` 에 **자동 저장**한다. 같은 문제는 새 줄을
  쌓지 않고 **횟수+최근시각**으로 병합(merge_window). 대시보드에서 표로 조회·
  새로고침·비우기. 서버 `GET /api/troubleshoot/history`,
  `POST /api/troubleshoot/history/clear`.
- **📈 디렉터리 읽기 속도 변화 차트** — 진단할 때마다 초당 디렉터리/파일 처리량을
  시계열로 누적해 차트로 그린다(자동 3초로 연속 추적). 각 선은 자기 최댓값 기준이라
  **변화량(오르내림)** 을 보기 좋다. 현재값·피크 표시.

---

## [1.26.0] - 2026-06-11

### 추가됨 (Added) — "튜닝 점검" 메뉴 (OS·마운트 가속 분석)
- **`🔧 튜닝 점검`** 메뉴 + **`tunecheck` CLI** — 거대 NAS 스캔을 빠르게 만드는
  OS/마운트 설정을 분석해 항목별 현재값·권장값·상태(ok/주의/권장)·적용 명령을 낸다.
- **NFS 마운트(대상)**: `nconnect`(서버당 TCP 다중화 — 메타데이터 병렬의 핵심),
  NFS 버전, `rsize/wsize`, 속성 캐시(`actimeo`/`noac`), `hard/soft`, `noatime`.
- **커널/sysctl**: `sunrpc.tcp_slot_table_entries`(동시 in-flight RPC), `vm.swappiness`,
  `vm.vfs_cache_pressure`, `fs.file-max`, `net.core.rmem_max`.
- **프로세스 한도**(open files), **CPU 거버너**(performance), **로컬 DB 디스크**
  (SSD 여부·DB가 NFS 위에 있으면 경고).
- `isilon_usage/systune.py` + `tests/test_systune.py`. 서버 `GET /api/systune`.

---

## [1.25.0] - 2026-06-11

### 추가됨 (Added) — "트러블슈팅" 메뉴 (실행 중 병목 진단)
- **`🩺 트러블슈팅`** 메뉴 — "검색이 느리다"고 느낄 때 실행 중인 스캔을 약 1초간
  들여다보며 **어느 구간이 병목인지** 진단한다.
- **구간 진단**: serve 모드 스캔은 서버 프로세스의 스레드(`disc-*`)라
  `sys._current_frames()` 로 각 워커가 지금 어느 코드 구간(디렉터리 읽기/파일
  stat/하위 합산/DB 쓰기/유휴)에 있는지 분류 → 직렬화·굶주림·NAS 행·DB 커밋
  바운드 등 **판정 + 처방**.
- **자원 병목 점검(CPU·메모리·디스크·NAS)**: 프로세스 CPU%(GIL 1코어 포화 감지)·
  시스템 iowait·메모리/스왑·로컬 디스크 여유·**NAS 메타데이터 응답 지연**(스레드+
  타임아웃 프로브로 행 감지)을 재서 **가장 제약인 자원**을 짚어 준다.
- **처리량**(초당 디렉터리·파일)·**하트비트**·**대기 일감(pending) 추정**·**워커별
  현재 경로**(멈춘 경로 찾기)를 함께 표시.
- `isilon_usage/troubleshoot.py` + 서버 `GET /api/troubleshoot`.

---

## [1.24.0] - 2026-06-11

### 추가됨 (Added) — "대상 분석" 메뉴 (풀스캔 전 깊이 구조 측정)
- **`🧭 대상 분석`** 대시보드 메뉴 + **`analyze` CLI** — 풀스캔 *전에* 디렉터리
  **깊이 구조만** 빠르게 측정(파일 stat·DB 없이 폴더만 병렬 readdir → 풀스캔보다
  훨씬 빠름). 깊이별 디렉터리 수·누적(fold 시 보존 행)·최대 깊이를 보여주고
  **추천 fold-depth** 를 계산한다(폭발이 시작되는 깊이 바로 위).
- **⏱ 샘플 수집 시간(시간/분/초)** — 디렉터리가 수천만 개여도 **지정한 시간만**
  수집하고 멈춘다(초과 시 '하한 추정치'로 표시). 0/0/0=무제한. 최대 깊이 캡·
  방문 개수 한도도 지원(`--max-depth`/`--limit`/`--timeout`/`--workers`).
- 결과에서 추천 fold-depth 를 **설정에 바로 적용**하는 버튼 + 실행 명령 예시.
- `isilon_usage/analyzer.py`(병렬·예산 기반 깊이 프로브) + `tests/test_analyzer.py`.
- 서버 API: `POST /api/analyze/start`·`/api/analyze/stop`, `GET /api/analyze/status`.

---

## [1.23.2] - 2026-06-09

### 추가됨 (Added) — 외부 배포 준비
- **MIT License** 추가(`LICENSE`) — 외부 배포의 법적 전제. `setup.cfg` 에 license
  메타/분류자, `MANIFEST.in`·릴리스 아카이브에 포함.
- **`SECURITY.md` 하드닝 가이드** — 위협 모델·기본 동작 주의점·외부 노출 시 필수
  조치(로컬 바인드+SSH 터널, `--mount-base`, `--lock-settings`, 작업 보호 비밀번호,
  TLS 프록시, 읽기전용 마운트, 최소 권한)·민감 파일(`info.MD`/`settings.json`).
- README 헤더에 보안 가이드·MIT License 링크.

---

## [1.23.1] - 2026-06-09

### 추가됨 (Added)
- 요약에 **'완료 예상시각'** 추가 — 남은 시간(ETA)뿐 아니라 **끝나는 절대 시각**을
  표시(오늘이면 `오늘 HH:MM`, 다른 날이면 `M/D HH:MM`). 프론트 전용 변경이라
  `dashboard.html` 만 교체하면 적용된다.

---

## [1.23.0] - 2026-06-09

### 추가됨 (Added) — 커뮤니티 추천 #4·#5·#8·#10
- **🐘 최대 파일 Top-N**: 스캔 중 상위 200개 파일을 힙으로 유지(메모리 미미),
  주기 저장·재개 복원. 분석 리포트에 표(경로·크기·수정시각·소유자), `/api/stats`
  의 `top_files`, CLI `stats --top N`. 스키마 8(`top_files` 테이블).
- **📈 용량 소진 예측**: 같은 루트의 완료 스캔 이력으로 선형 추세(증가/일)를
  계산해 **FS 90% 도달·가득 참 예상일**을 보여준다(2회+ 스캔 필요).
  `GET /api/forecast?scan=ID`.
- **🔀 변화 Top(직전 스캔 대비)**: 기존 diff 를 발전 — 가장 많이 **커진/줄어든/
  신규/삭제** 디렉터리를 자동으로 골라 보여준다. `GET /api/growers?scan=ID`.
- **Prometheus `/metrics`**: 루트별 `scanned_bytes`/`total_files`/`running`/
  `heartbeat_age_seconds`/`fs_{total,used,free}_bytes` + `isilon_usage_info{version}`
  를 텍스트 포맷으로 노출(stdlib 구현) — 기존 Grafana 에 바로 연동.

---

## [1.22.0] - 2026-06-09

### 추가됨 (Added)
- **분석 리포트 — 파일 나이 / 소유자 / 확장자별 사용량**(아이실론 커뮤니티 요구 반영):
  - 스캔하며 파일마다 **수정시각(나이)·소유자(uid)·확장자**별로 용량/개수를 누적해
    **콜드 데이터·차지백·유형 분석**을 제공한다. 추가 패스 없이 기존 `stat` 결과를 집계해
    비용이 거의 없다(하드링크는 용량 1회, 개수는 엔트리 단위).
  - 스키마 7: `scan_stats(run_id, kind, key, bytes, files)` 테이블. 주기적·종료 시
    저장하고 **재개 시 복원**해 합산이 정확하다. 메모리는 distinct 키 상한으로 묶음.
  - 대시보드 **`📊 분석 리포트`** 탭(나이 분포·소유자 Top·확장자 Top 막대),
    API `GET /api/stats?scan=ID`(소유자 uid→이름 변환), CLI `stats [--scan]`.

---

## [1.21.1] - 2026-06-09

### 변경됨 (Changed)
- **작업 잠금 해제 토큰 유효시간을 10초로** 단축(기존 24시간). 비밀번호 입력 후
  **10초 동안만 자동 통과**하고 이후엔 다시 잠긴다(서버가 강제).
- 비밀번호 입력 직후 **"N초 동안 비밀번호 자동 입력" 팝업(토스트)** 을 띄우고,
  헤더 배지에 **남은 초를 카운트다운**한다. 만료되면 토큰을 비우고 다시 잠금 표시.
  `POST /api/unlock` 응답에 `ttl` 포함.

---

## [1.21.0] - 2026-06-09

### 추가됨 (Added)
- **작업 보호(비밀번호 잠금)** — 보기는 자유, 작업은 비밀번호:
  - 설정 `🔒 작업 보호`에서 비밀번호를 걸면 **모든 작업(POST: 스캔 시작/중지/재개/삭제,
    설정 저장, 테스트 데이터 생성, 실측 보정 등)에 비밀번호가 필요**하다. **보기(GET)는
    그대로 자유.** 비밀번호 미설정이면 잠금 없음(기존과 동일).
  - 잠금 해제 시 토큰 발급(`POST /api/unlock`) → 이후 작업은 `X-Op-Token` 헤더로 통과.
    대시보드는 fetch 를 감싸 **POST 에 토큰을 자동 첨부하고, 잠겨 있으면(401) 비밀번호를
    물어 자동 해제 후 재시도**한다. 헤더의 🔒/🔓 배지로 상태 표시·클릭 해제/재잠금.
  - 비밀번호는 **설정에서 변경/해제**(체크 해제 후 저장 시 보호 끔), 그리고 **실행한
    디렉터리의 `info.MD`(권한 600)** 에 기록된다(분실 대비). `op_password` 는 응답에서
    마스킹(`op_password_set` 만 노출).
  - ⚠ HTTP 평문 전송이므로 강력 인증이 아니라 **실수·오작동 방지용 운영 게이트**다.

---

## [1.20.0] - 2026-06-09

### 추가됨 (Added)
- **테스트 데이터 생성기(`🧪 테스트 데이터` 메뉴 / `gentest` CLI)** — 스캐너 시험용
  샘플 트리 생성:
  - 대상 디렉터리에 **N개 디렉터리 → 각 M개 하위 디렉터리 → 각 폴더에 K개 파일
    (크기 S)** 을 만든다(각 폴더 = 상위 N + 하위 N×M). 총 디렉터리/파일/용량을
    미리 보여준다.
  - 새 모듈 `gentest.py`(검증·생성), API `POST /api/gentest/{validate,start,stop}` +
    `GET /api/gentest/status`(백그라운드 생성 + 진행 폴링), 대시보드 전용 화면
    (입력값·예상 합계·진행 막대·중지), CLI `gentest <path> --dirs --subdirs --files
    --size(0/4K/1M…) [-y]`.
  - **안전장치**: 시스템 경로(`/etc` 등) 거부, 쓰기 권한·디스크 여유 확인, 총 파일
    수 상한(5천만). ⚠ 실제로 파일을 쓰므로 쓰기 가능한 경로를 지정해야 한다.

---

## [1.19.2] - 2026-06-09

### 변경됨 (Changed)
- **요약의 'DB 크기' 값 위치 고정** — 뒤따르는 상태 문구(증가 중/변화 없음) 길이가
  바뀌어도 숫자(예: `381 MB`)가 좌우로 밀리지 않게 **오른쪽 끝에 고정**(다른 숫자
  행과 정렬). 상태 문구는 숫자 왼쪽에 둔다.
- **실행 중 스캔의 스레드 수와 설정이 다르면 안내 표시** — `Multi Scan N Thread`
  옆에 `· 설정 M (중지→재개 시 적용)` 을 띄워, **설정 변경은 실행 중 스캔에 즉시
  반영되지 않고 새 스캔/재개부터 적용**됨을 명확히 한다. (예: 8로 시작한 스캔은
  설정을 32로 바꿔도 8로 계속 돌고, **중지 후 재개**하면 32로 이어진다.)

---

## [1.19.1] - 2026-06-09

### 고쳐짐 (Fixed)
- **실측 보정이 8 스레드에서 멈추던 문제** 수정. 메모리 2배 판정 기준을 '유휴
  시점 RSS'로 잡아, 실제 대용량에서는 **첫 8-스레드 측정만으로도 2배를 넘어
  거기서 escalation 이 중단**되던 버그. 기준을 **'한 번 스캔했을 때의 메모리
  (워밍업 측정 peak)'**로 바꾸고, 최소 한 후보는 측정한 뒤에만 중단하도록 해
  8 → 16 → 32 … 로 정상적으로 올라가게 했다.

---

## [1.19.0] - 2026-06-09

### 변경됨 (Changed)
- **실측 보정(시범 스캔) 개선** — 메모리 한도 단계 상승 + 단계별 결과 표시:
  - 스레드를 **오름차순(8→16→32→48→64)으로 올리며**, 측정 중 프로세스 RSS 가
    **시작 대비 2배(`--mem-factor`)에 도달하면 더 올리지 않고 중단**한다. 각 후보의
    **peak RSS·배수**를 함께 보여줘 메모리 영향을 눈으로 확인할 수 있다.
  - 결과를 **한 번에 몰아 보여주지 않고**, 각 후보 측정이 끝날 때마다 **단계별로
    추가 표시**한다. 대시보드는 백그라운드 시작(`POST /api/benchmark-workers/start`)
    후 `GET /api/benchmark-workers/status` 를 폴링해 행을 하나씩 채운다. CLI
    `tune --benchmark` 도 단계별로 출력하고 `--mem-factor` 옵션을 받는다.
  - 기존 동기 엔드포인트(`POST /api/benchmark-workers`)는 호환을 위해 유지.

---

## [1.18.1] - 2026-06-09

### 변경됨 (Changed)
- **하트비트 표시를 '마지막 체크 시각 + 다음 새로고침 카운트다운'으로 개선**:
  - 기존 "N초 전 갱신" 대신 **마지막 체크(스캐너 갱신) 절대 시각**(예: `14:23:05`)과
    경과 초를 함께 표시하고, **다음 새로고침까지 남은 초를 0.5초 간격으로 카운트다운**.
  - 카운트다운이 멈추면 **브라우저(프론트)가 멈춘 것**, '초 전'만 계속 늘면 **스캐너가
    멈춘 것**으로 구분 진단할 수 있다(자동 새로고침이 꺼져 있으면 그 상태도 표시).
  - 프론트 전용 변경 — `dashboard.html` 만 교체하면 적용(스캔 재시작 불필요).

---

## [1.18.0] - 2026-06-09

### 추가됨 (Added)
- **재개 시 '누적 작업 시간'과 '이번 재시작 후' 시간을 각각 표시**:
  - 스키마 6: `scan_runs` 에 `session_started_at`(이번 세션 시작)과
    `elapsed_accum`(모든 세션 누적 활성 시간) 추가.
  - 누적 시간은 진행 갱신마다 증분 적립해 **일시정지 갭은 빼고** 실제 작업 시간만
    재며, **`kill -9` 에도 마지막 갱신까지 보존**된다(재개 시 그 위에 이어서 적립).
  - 요약에 **경과 시간(누적 작업)** 과, 재개된 스캔이면 **이번 재시작 후**(처음 시작
    시각 포함)를 각각 표시. `/api/status` 에 `session_started_at`·`elapsed_accum` 포함.
- **로컬 저장 공간 여유 표시(DB·로그)**:
  - 요약에 **DB 디스크 여유**(데이터 폴더가 속한 로컬 FS)와 **로그 디스크 여유**
    (로그 경로가 다른 FS일 때)를 사용률 색상과 함께 표시 → DB 폭증·디스크 부족을
    조기에 감지. `/api/status` 에 `local_disk{db,log}` 포함.

### 변경됨 (Changed)
- 요약의 **'DB 구성 · 한도'** 에서 '한도 없음' 안내 문구를 **줄바꿈**해 보기 좋게 정리.

---

## [1.17.0] - 2026-06-09

### 추가됨 (Added)
- **실측 보정(시범 스캔)으로 최적 스레드 수 추천** — 사양 heuristic 보완:
  - 대상 경로에서 후보 스레드 수(기본 8/16/32)로 **짧게 시범 탐색**해 초당 디렉터리
    처리량을 비교하고 가장 빠른(또는 무릎점) 값을 추천한다. 시범 스캔은 로컬 임시
    DB 에 기록 후 즉시 삭제하므로 관리 DB/실제 스캔에 영향이 없다.
  - 워밍업 + 후보 순서 무작위화로 NFS 캐시 편향을 완화하고, 결과는 '추정치'로 안내.
  - 새 모듈 `tuning.py`, CLI `tune --benchmark PATH [--candidates 8,16,32] [--budget 5]`,
    API `POST /api/benchmark-workers`(경로는 허용 마운트로 검증), 설정 화면
    **'실측 보정(시범 스캔)'** 버튼(표로 결과 표시 후 한 번에 적용).

### 변경됨 (Changed)
- **스토리지 어레이 상태 카드** — 아이실론/PowerStore 등이 **하나도 설정되지 않으면
  카드 자체를 숨긴다**(이전엔 '미설정' 안내를 표시). 설정하면 다시 나타난다.

---

## [1.16.1] - 2026-06-09

### 고쳐짐 (Fixed)
- **대시보드 상세가 `ReferenceError: data is not defined` 로 안 뜨던 회귀 수정**
  (1.16.0 의 DB 생존 지표 렌더 코드가 잘못된 변수명 `data` 를 참조). `render()`
  파라미터 `d` 로 정정. `dashboard.html` 만 교체하면 적용(스캔 재시작 불필요).

### 변경됨 (Changed)
- **상세 갱신 지연/오류 처리 견고화**:
  - 데이터 수신(네트워크/타임아웃)과 화면 렌더(코드 오류)를 **분리 처리**.
  - 네트워크 지연은 **2회 연속부터** 부드럽게 안내(거대 DB 에서 정상일 수 있음을 명시,
    한 번의 일시 지연으로 빨갛게 깜빡이지 않음). 렌더 오류는 콘솔 로그 + 별도 안내로
    구분하고, **오류가 나도 폴링이 멈추지 않게** `render()` 를 try/catch 로 감쌈.
  - 정상 처리되면 이전 오류/지연 안내를 자동으로 지움.

### 문서 (Docs)
- USER_GUIDE FAQ 에 **"프로그램 실행 후 `df` 에 마운트가 많아져요(Isilon crossmnt)"**
  항목 추가 — 자동 서브마운트는 정상이며, `--one-file-system` 을 켜면 중첩 익스포트가
  누락되니 끄라는 주의 포함.

---

## [1.16.0] - 2026-06-09

### 추가됨 (Added)
- **요약에 DB 생존 지표(`db_usage`) 표시** — DB 가 살아있는지 한눈에:
  - 현재 사용 중인 per-run DB 사용량(`.db` + `-wal` + `-shm`)을 요약에 표시하고,
    폴링마다 값이 **커지면 🟢 증가 중(쓰는 중=살아있음)**, 변화 없으면 ⚪ 표시.
  - 구성 분해(`.db`/`WAL`)와 **DB 최대 GB 한도 대비 사용률(%)** 도 함께 보여줘
    DB 폭증을 조기에 감지한다. `/api/status` 에 `db_usage{db_bytes, wal_bytes,
    shm_bytes, total_bytes, limit_bytes}` 포함.
- **서버 사양 기반 권장 스레드 계산** — "몇 개 스레드가 최선?"을 자동 계산:
  - 서버의 논리 CPU 수·가용 메모리·백엔드를 보고 **권장 동시 스캔 스레드 수**와
    근거를 계산한다. native 스캔은 NFS I/O 대기가 대부분이라 코어 수보다 많은
    스레드(≈코어×4)가 유리하고, du 백엔드는 프로세스 부하로 코어 수 근처가 적당.
    가용 메모리·설정 상한(64)으로 자동 클램프.
  - CLI `python -m isilon_usage tune [--backend native|du]`,
    API `/api/recommend-workers`, 설정 화면 **'서버 사양 기반 권장 계산'** 버튼
    (계산 결과를 보여주고 한 번에 적용).

---

## [1.15.0] - 2026-06-09

### 추가됨 (Added)
- **깊이 접기(fold_depth) — DB 크기를 묶으면서 합계는 정확**:
  - 깊이 N 까지만 디렉터리 **행을 저장**하고, 그 아래는 **내려가서 용량은 다 세되**
    깊이 N 디렉터리의 `own_bytes` 에 합산한다 → **DB 행 수(=크기)가 묶이고**
    상위 합계는 **무제한 스캔과 정확히 동일**하다(N 아래 디렉터리별 상세만 없음).
  - 기존 `--max-depth` 는 깊이 초과를 아예 방문하지 않아 **깊은 용량이 합계에서
    누락**되던 한계가 있었는데, 깊이 접기는 이를 해결한 권장 방식이다.
  - 설정 `fold_depth`, CLI `--fold-depth N`, 설정 화면 입력 추가.
- **DB 크기 가드(db_max_gb) — 자동 일시정지**:
  - per-run DB(`.db` + `-wal`)가 설정 GB 를 넘으면 스캔을 **자동 일시정지**하고
    사유를 기록한다(WAL 체크포인트 직후 측정 → 실제 영속 크기에 근접).
  - 설정 `db_max_gb`, CLI `--db-max-gb N`, 설정 화면 입력 추가.
  - 깊이 접기로 DB 를 작게 유지해 끝까지 스캔하고, 이 가드는 만일의 폭증을 막는
    안전망으로 함께 쓰는 것을 권장한다.

---

## [1.14.0] - 2026-06-08

### 추가됨 (Added)
- **서버 죽음 방지 안전장치(수십억 파일·초대용량 대비)**:
  - **디스크 여유 자동 일시정지**: 데이터 디스크 여유가 설정값(GB) 미만이면
    스캔을 **자동으로 일시정지**하고 사유를 기록한다(설정 `min_free_gb`, 0=끔).
    DB·WAL 이 디스크를 가득 채워 서버가 죽기 전에 안전하게 멈춘다.
  - **탐색 최대 깊이 제한**: `scan_max_depth` 로 그 아래는 탐색/저장하지 않아
    DB 행 수와 크기를 묶는다(0=무제한).
  - **하드링크 중복 제거 토글**: `hardlink_dedup` 을 끄면 `(dev,ino)` 집합을
    메모리에 쌓지 않아, 하드링크가 많은 트리에서 메모리를 절약한다(용량은 중복 셈).
  - 설정 화면에 세 항목 입력 UI 추가.

### 고쳐짐 (Fixed)
- **대시보드 폴링 분리/타임아웃** — 거대 DB 에서 느린 상세 요청(`/api/status`)이
  쌓여 브라우저 연결을 막고 관리 개요까지 멈추던 문제 수정:
  - 개요(`/api/scans`)와 상세(`/api/status`)를 분리해, 개요는 항상 먼저 갱신한다.
  - 상세는 **한 번에 하나만**(in-flight 가드) 보내고 **타임아웃**(개요 8초·상세 25초)을
    둬, 응답이 늦어도 폴링이 누적되지 않는다.
  - 이 수정은 `dashboard.html` 만 교체하면 적용된다(스캔 재시작 불필요).

---

## [1.13.0] - 2026-06-08

### 고쳐짐 (Fixed)
- **거대 스캔에서 대시보드 상세(`/api/status`)가 멈춘 듯 느려지던 문제** 수정:
  - 대시보드가 쓰지 않는 상위 디렉터리 정렬(전체 테이블 정렬)을 `/api/status` 에서
    **생략(`top=0`)** → 매 폴링이 가벼워진다(CLI `status` 는 그대로 표시).
  - 스캐너가 **주기적으로 WAL 체크포인트**(`wal_checkpoint(TRUNCATE)`, 30초)하여,
    느린 읽기 락 때문에 `-wal` 파일이 수 GB 까지 부풀던 악순환을 막는다.

### 추가됨 (Added)
- **진행 갱신(하트비트) 표시**: 요약에 마지막 진행 갱신 경과를 보여줘 스캐너 생존을
  바로 확인한다(🟢 N초 전 정상 / 🔴 60초+ 멈춤 의심). `/api/status` 에
  `updated_at`·`server_time` 포함.

---

## [1.12.0] - 2026-06-08

### 추가됨 (Added)
- **스토리지 어레이 5종 추가 — Dell Unity / PowerMax / VMAX / XtremIO / VPLEX**:
  각 REST API 로 클러스터 이름·버전/모델·노드 수·용량·건강을 조회한다(유형별
  디스패처 `storage_status`). 설정의 **"추가 스토리지 어레이"** 표에서 유형을 골라
  주소·계정을 등록하면 **"스토리지 어레이 상태"** 카드와 **HQ 포탈 노드**에 함께
  표시된다(어레이별 배지 `UNI/PMAX/VMAX/XIO/VPLEX`, 비밀번호 마스킹·보존).
  표준 라이브러리만 사용. 실제 응답 필드는 장비/펌웨어 버전에 따라 다를 수 있어
  안 맞는 항목은 베스트 에포트로 degrade 한다(샘플로 정밀 매핑 가능).

---

## [1.11.0] - 2026-06-08

### 추가됨 (Added)
- **Dell PowerStore 상태 조회**: PowerStore REST API 로 클러스터 이름·상태,
  어플라이언스 모델, 노드 수, 용량(space metrics), 활성 알림(건강)을 가져온다.
  아이실론과 함께 **"스토리지 어레이 상태"** 카드 하나로 통합 표시
  (`GET /api/storage`, 서버 60초 캐시). 설정에 PowerStore 주소·계정·인증서 검증
  항목 추가(읽기 전용 권장, 비밀번호 마스킹). 표준 라이브러리만 사용.
- **HQ 포탈 글로벌 DC 어레이 상태**: 노드 행에 어레이별 배지(`ISI`/`PS` ✓·⚠·✕) +
  펼침에 어레이별 상세. 아이실론·PowerStore 모두 dbexport meta 로 수집.

### 변경됨 (Changed)
- 대시보드 "아이실론 클러스터 상태" 카드를 **"스토리지 어레이 상태"** 로 일반화해
  여러 어레이(아이실론·PowerStore)를 한 카드에 나열한다(`/api/isilon` 호환 유지).

---

## [1.10.0] - 2026-06-08

### 추가됨 (Added)
- **아이실론(OneFS) 클러스터 상태 조회**: OneFS Platform API(PAPI)로 클러스터
  **이름·버전·용량·노드 수·미해결 이벤트(건강)** 를 가져와 대시보드 "아이실론 클러스터
  상태" 카드에 표시한다(`GET /api/isilon`, 서버 60초 캐시). 설정에서 PAPI 주소·계정·
  인증서 검증 여부를 입력(읽기 전용 계정 권장, 비밀번호 마스킹). 표준 라이브러리만
  사용(urllib+ssl, 자체 서명 인증서 허용).
- **HQ 포탈에서 글로벌 DC 아이실론 상태**: 각 엣지의 아이실론 상태를 dbexport
  meta 에 실어 포탈이 수집 → 글로벌 대시보드 노드 행에 **`ISI ✓/⚠/✕`** 배지 +
  펼침에 클러스터 상세(이름·버전·노드·용량·이벤트)를 표시한다.

---

## [1.9.0] - 2026-06-08

### 추가됨 (Added)
- **"지금 조사 중인 디렉터리"를 워커별로 표시**: 병렬 탐색 시 각 워커가 보고 있는
  디렉터리를 모두 보여준다(`worker_dirs`, **스키마 5**, 구버전 DB 자동 마이그레이션).
  **모두 / N개** 표시는 체크박스 + 숫자 입력으로 사용자가 지정한다(가변 워커 수 대응).
- **브라우저 탭 제목에 스캔 상태**: 스캔 중에는 탭 제목이 `● 스캔중 · …` 로 바뀐다.

### 변경됨 (Changed)
- 헤더의 스크롤(마퀴) 표시는 레이아웃이 깨질 수 있어 제거하고, 위 두 가지(워커별
  목록 + 탭 제목)로 대체했다.

---

## [1.8.1] - 2026-06-08

### 추가됨 (Added)
- **헤더에 현재 스캔 디렉터리 마퀴(스크롤)**: 스캔 중(탐색/집계)에는 제목 아래에
  `🔎 스캔 중: <현재 디렉터리>` 가 좌→우로 스크롤되어 진행 위치를 한눈에 보여준다
  (마우스를 올리면 멈춤). 스캔이 끝나면 숨겨진다.

---

## [1.8.0] - 2026-06-08

### 추가됨 (Added)
- **포탈 글로벌 대시보드 폴리시**: 지역 카드 **클릭 필터**, 노드 표 **머리글 정렬**
  (이름/지역/스토리지/사용/상태), "마지막 갱신" 표시.

### 변경됨 (Changed)
- **대시보드 로딩 플레이스홀더**: 첫 데이터 도착 전 자원 카드의 큰 숫자를 `0%`/`0 B`
  대신 **`—`** 로 표시(오해 방지).
- **Summary 진행 안내**: 탐색 단계에서 "모든 디렉터리를 찾은 뒤 집계가 시작됩니다"
  안내를 진행 카드에 표시.

### 문서 (Docs)
- **USER_GUIDE.md 갱신**: 글로벌 통합 포탈(12장), 최근 추가 기능 요약(13장, 화면 분리·
  예약 반복주기·읽기전용·병렬 표시·고아 정리·경로 별칭), `portal` CLI 추가.

---

## [1.7.1] - 2026-06-08

### 추가됨 (Added)
- **경로 비교 — 경로 별칭(alias)**: 노드마다 `로컬 마운트 접두어 ↔ 공통 논리 접두어`
  를 지정해, **마운트 접두어가 다른 DC도 같은 논리 경로로 비교**할 수 있다.
  (예: `/mnt/isilon/data` ↔ `/data`). 비교 입력은 **논리 경로**이고, 노드별로 로컬
  경로에 매핑해 복제본 DB를 조회한다(매트릭스는 자식 이름 기준 병합). 별칭 미설정
  시 기존대로 절대경로 일치. 포탈 "노드 설정"에 별칭 입력란 추가.

---

## [1.7.0] - 2026-06-08

### 추가됨 (Added)
- **글로벌 포탈 — 경로 비교(Cross-DC, Phase 4)**: 복제본 DB를 경로로 조인해 같은
  경로의 노드별 용량/파일 수를 비교한다 (`GET /api/portal/compare`,
  `GET /api/portal/matrix`).
  - **단일 경로 비교**: 노드별 사용 용량·파일 수 + 최대 대비% + **드리프트/누락 판정**.
  - **통합 디렉터리 매트릭스**: 행=디렉터리, 열=노드. 중앙값 대비 편차·누락을 강조하고,
    디렉터리를 클릭하면 더 깊이 드릴다운한다. 포탈에 **"경로 비교" 탭** 추가.
  - 비교는 **절대경로 동일** 기준(마운트 접두어가 다른 노드는 '없음'). 경로 별칭은 추후.
- **글로벌 대시보드 노드 펼침(Phase 3)**: 노드 행을 클릭하면 그 노드의
  **스토리지(루트) 목록**(사용/용량/상태)이 펼쳐진다.

---

## [1.6.0] - 2026-06-08

### 추가됨 (Added)
- **글로벌 포탈 노드 복제 주기에 "시작 + 반복주기" 모델 적용**: 노드별 복제(동기화)를
  **분 / 시간 / 일 / 주 / 개월** 단위로 지정할 수 있다(예약 스캔과 동일 모델,
  `schedule_due` 재사용). 포탈 노드 설정에 반복주기 선택기 + 요약을 추가했다.
  - 구버전 `interval_minutes` 노드는 `unit=minute` 로 자동 호환.
  - 연결 폴링(온라인/요약)은 기존대로 자주 수행하고, **복제 시점만** 반복주기로 판정.

---

## [1.5.0] - 2026-06-08

### 추가됨 (Added)
- **대시보드 "분석"을 "디렉터리" + "추세·비교" 로 추가 분할**:
  - **디렉터리**: 디스크 사용량 파이 + 상세 보기 스캔 + 용량 상위 디렉터리 +
    디렉터리 탐색(드릴다운).
  - **추세·비교**: 용량 추세 + 스캔 비교 + 오류 디렉터리.
  - 네비게이션: **Summary / 디렉터리 / 추세·비교 / ⚙ 설정 / 📖 버전 기록**.

### 고쳐짐 (Fixed)
- **고아(orphan) 스캔 자동 정리**: 대시보드 서버를 재시작하면 이전 프로세스의 스캔
  스레드는 죽지만 DB 상태가 '탐색중'으로 남아 "탐색중인데 실제로는 아무것도 안
  도는" 모순이 생기던 문제 수정 — 서버 시작 시 실행 중이 아닌 `discovering`/`sizing`
  스캔을 **'일시정지'로 정리**한다('재개'로 이어갈 수 있음).

---

## [1.4.2] - 2026-06-08

### 고쳐짐 (Fixed)
- **새로고침 직후(첫 폴링 전) "새 스캔 시작" 카드가 잠깐 보이던 현상** 수정 —
  기본을 숨김으로 두고 폴링에서 "실행 중 스캔 없음"일 때만 노출한다. 스캔 중
  새로고침해도 이 카드가 뜨지 않는다.

### 추가됨 (Added)
- **분석 화면 진행 중 안내 배너**: 스캔이 진행 중(탐색/집계)일 때 분석 메뉴 상단에
  "집계 전이라 일부 값은 임시 표시 — 완료 후 정확해짐" 안내를 띄운다(완료 시 숨김).

---

## [1.4.1] - 2026-06-08

### 변경됨 (Changed)
- **대시보드를 "Summary" 와 "분석" 두 화면으로 분리**(상단 네비게이션):
  - **Summary**: 자원(서버 메모리 / 측정 프로세스 / CPU·디스크) + 전체 용량 관리
    개요 + 현재 진행 상황 + 요약.
  - **분석**: 디스크 사용량 파이 + 용량 상위 디렉터리 + 디렉터리 탐색(드릴다운) +
    용량 추세 + 스캔 비교 + 오류 디렉터리.
  - 네비게이션: **Summary / 분석 / ⚙ 설정 / 📖 버전 기록** (기본 화면 = Summary).

---

## [1.4.0] - 2026-06-08

### 추가됨 (Added)
- **예약 스캔 반복 주기 — "시작 + 반복주기" 모델**: 기존 "N분마다" 외에
  **분 / 시간 / 일 / 주 / 개월** 단위로 반복을 지정할 수 있다.
  - **주(Weekly)**: 시작 요일(복수 선택) + 매 **Y주**마다 + 실행 시각.
  - **개월(Monthly)**: 시작 **x월 x일** + 매 **Y개월**마다 + 실행 시각.
  - 일(Daily)/시간도 "시작 기준 + 반복주기"로 지정. 설정의 예약 편집기에 단위
    콤보박스 + 조건 입력 + 사람이 읽는 요약("월요일 03:00 · 매 2주마다")을 추가.
  - 스케줄러가 `schedule_due` 로 단위별 다음 실행을 판정한다(구버전 `every_minutes`
    예약은 `unit=minute` 로 자동 호환).

---

## [1.3.1] - 2026-06-08

### 변경됨 (Changed)
- **마운트 읽기전용(ro) 확인을 설정에서 켜기/끄기**(`check_readonly`, 기본 켬).
  끄면 스캔 시작 시 검사하지 않고(`mount_readonly=-1`), 요약의 "마운트 상태" 행과
  시작 메시지의 ro/rw 표시를 숨긴다. `/api/status` 는 `readonly_checked` 로 표시
  여부를 알린다.

### 문서 (Docs)
- 예약 스캔 **반복 주기 목업을 "시작 + 반복주기" 구조로 구체화**
  (Monthly: x월 x일부터·y개월마다 / Weekly: 시작 요일·y주마다).

---

## [1.3.0] - 2026-06-08

### 추가됨 (Added)
- **글로벌 통합 포탈(HQ) — `portal` 모드(Phase 2)**: `python3 -m isilon_usage portal`
  로 여러 데이터센터(엣지)를 한 화면에서 조망한다.
  - **노드 설정**(웹): 글로벌 노드 **추가/수정/삭제 + 연결 테스트 + 즉시 동기화**.
    `portal_nodes.json` 에 저장(토큰 마스킹).
  - **백그라운드 동기화**: 각 노드를 폴링(연결·요약)하고 **완료된 per-run DB 를
    `replicas/<id>/` 로 증분 복제**(엣지 `/api/dbexport` 재사용, 토큰).
  - **글로벌 대시보드**: 전체/지역/노드별 사용량 롤업 + 온라인·오프라인·스캔중 상태 +
    각 DC 대시보드 열기.
- **현재 위치 사용 용량 표시**: "용량 상위 디렉터리"와 "디스크 사용량 파이"의
  빵부스러기(🏠 루트 / …) 뒤에 **현재 위치의 재귀 사용 용량**을 표시.

### 고쳐짐 (Fixed)
- **스캔 시작 직후 첫 `/api/status` 가 500** 나던 레이스(per-run DB 테이블 생성 전
  폴링)를 수정 — "초기화 중"으로 응답한다. 새로고침 직후 상세 화면이 잠깐 비어
  보이던 현상이 완화된다(`/api/topdirs` 도 동일 보호 → 빈 목록).

---

## [1.2.0] - 2026-06-08

### 추가됨 (Added)
- **글로벌 통합 포탈 — 복제 채널(Phase 1)**: 엣지에 `GET /api/dbexport` 추가.
  완료된 per-run DB(불변)와 노드 요약(`meta.json`)을 **tar.gz 로 증분(`?since=`)
  스트리밍**한다. 한국 HQ 포탈이 이를 받아 복제해 글로벌 조회/경로 비교의 토대로 쓴다.
  (진행 중 스캔 DB 는 쓰기 중이라 제외, 완료분만 — Python 3.6·구버전 SQLite 호환.)
- **API 토큰 인증(선택, `api_token`)**: 설정 시 `/api/dbexport` 에 `X-Auth-Token`
  헤더(또는 `?token=`)가 필요. 미설정 시 공개(기존과 동일). 토큰은 화면/응답에
  노출하지 않고 설정 여부만 표시. 대시보드 설정에 입력란 추가.
- **마운트 읽기전용(ro) 확인**: 스캔 **시작 시** 대상 마운트가 읽기 전용인지
  검사(`statvfs` `ST_RDONLY`)해, 시작 메시지와 요약의 **"마운트 상태"** 에
  🔒 읽기전용(ro)/✏ 쓰기가능(rw) 으로 표시. `scan_runs.mount_readonly`
  컬럼 추가(**스키마 4**, 구버전 DB 자동 마이그레이션).

### 변경됨 (Changed)
- **용량 상위 디렉터리**: 집계 전(탐색 중) 행의 `재귀 용량`을 직속 용량 임시값으로
  **흐리게(`~…`)** 표시해 "하위 미포함 임시값"임을 분명히 한다(스캔 완료 후 하위를
  포함한 재귀 용량으로 자동 전환). — 탐색 중 재귀=직접으로 보여 혼동되던 점 개선.

### 문서 (Docs)
- 글로벌 통합 포탈 **샘플 목업**(`docs/mockups/global_portal_mockup.html`):
  아키텍처 구성도 · 글로벌 대시보드 · 경로 비교(Cross-DC) · 노드 설정.

---

## [1.1.9] - 2026-06-08

### 변경됨 (Changed)
- "측정 프로세스" 카드의 병렬 처리 항목을 **한 줄에 들어가도록** 정리:
  라벨을 **`⚡ Multi Scan N Thread`**(설정된 동시 스캔 스레드 수 N 자동 반영),
  값을 **`N개 스레드 동작중`**(현재 활성 워커 수)으로 짧게 바꾸고 줄바꿈 방지
  (`white-space:nowrap`). 기존 `병렬 처리(동시 스캔 스레드)` / `8 / 8 스레드 동시
  처리 중` 표기가 두 줄로 접히던 문제 해결.

---

## [1.1.8] - 2026-06-08

### 추가됨 (Added)
- **디스크 사용량 파이(클릭 드릴다운)** 카드를 대시보드 상단에 추가:
  - 검색 루트 기준 **1단계 하위 디렉터리들의 용량 비중**을 파이로 보여주고,
    "N개 하위 디렉터리 · 합계 · 이 폴더 파일" 요약을 함께 표시한다.
  - 파이 **슬라이스·범례·빵부스러기(🏠 / …)** 를 클릭하면 그 디렉터리로
    **점점 더 깊이** 내려가며 구체적인 디스크 사용량을 추적할 수 있다
    (`GET /api/topdirs` 재사용, 별도 라이브러리 없이 canvas 로 그림).
  - 집계가 끝나면 **‘기타(나머지 하위)’·‘이 폴더의 파일’** 슬라이스로 100% 를
    채워 정확한 비율을 표시한다(탐색 중에는 임시 표시).
- `GET /api/topdirs` 응답에 기준 디렉터리 요약(`parent`: 하위 개수·자기 파일
  용량·재귀 용량 등) 추가 — 파이의 개수/합계/기타 계산에 사용.

---

## [1.1.7] - 2026-06-08

### 추가됨 (Added)
- **용량 상위 디렉터리 — 깊이 선택 · 클릭 드릴다운 · 정렬**(`GET /api/topdirs`):
  - **깊이**: 검색 루트(또는 현재 위치) 기준 **1번째 자식, 2번째 자식 …** 또는
    "전체 깊이" 를 골라 볼 수 있다.
  - **드릴다운**: 경로를 클릭하면 그 디렉터리 하위로 들어가고, 빵부스러기(🏠 /
    …)로 되돌아온다. 점점 좁혀가며 구체적인 디스크 사용량을 추적할 수 있다.
  - **정렬**: 표 머리글(경로/깊이/재귀 용량/직접 용량/파일 수/하위디렉터리)을
    클릭해 오름/내림차순 정렬(서버에서 화이트리스트 컬럼으로 안전 정렬).
- **병렬 처리(동시 스캔 스레드) 수를 대시보드에 표시** — native 백엔드가 한
  프로세스에서 여러 디렉터리를 동시에 훑는 장점을 "측정 프로세스" 카드에
  `현재 활성 / 설정` 형태로 보여준다. `scan_runs` 에 `workers`·`active_workers`
  컬럼 추가(**스키마 3**, 구버전 DB 자동 마이그레이션).

### 변경됨 (Changed)
- **스캔 시작 시** 진행 화면만 남도록 **"상세 보기 스캔" 바**와 **"용량 추세 /
  스캔 비교(DIFF)" 카드**도 함께 숨긴다(완료 후 다시 표시).
- **예약 스캔 경로**를 자유 입력 대신 **기존에 스캔한 루트 드롭다운**에서
  선택하도록 변경(오타·미존재 경로 예약 방지).
- **초기 설치 시 동시 스캔 스레드 기본값을 8** 로 상향(NFS 대용량 트리 가속).

### 고쳐짐 (Fixed)
- **`BrokenPipeError`(브라우저가 응답 도중 연결을 끊음 — 잦은 폴링·새로고침)**
  로 콘솔에 트레이스백이 쌓이던 문제 해결. 응답 전송/파일 내보내기/요청 처리에서
  연결 끊김(`ConnectionError`/`OSError`)을 정상 상황으로 보고 조용히 무시한다.

---

## [1.1.6] - 2026-06-08

### 추가됨 (Added)
- **디렉터리 단위 병렬 탐색**: `scan_workers` 개의 워커 스레드가 'pending'
  디렉터리를 나눠 맡아 **여러 디렉터리를 동시에 scandir/stat**(느린 NFS I/O
  병렬화). 단일 RLock 으로 DB 접근·진행 카운터만 보호하고 I/O 는 락 밖에서
  실행해 **데드락 없이** 가속한다. 메모리는 워커별로 한 청크만 사용(여전히 최소).
  결과는 순차 처리와 동일하며 `du` 와 바이트 단위로 일치(workers 1/4/8 검증).
  (기존 "파일 stat 동시 처리"를 디렉터리 병렬로 대체 — 기본 4)

> NFS 대용량 트리에서 워커 수를 8~16 으로 올리면 탐색이 크게 빨라집니다.
> 단, 파일이 한 디렉터리에 몰려 있으면(예: 단일 거대 디렉터리) 그 디렉터리는
> 한 워커가 맡으므로 디렉터리가 많은 트리에서 효과가 큽니다.

---

## [1.1.5] - 2026-06-08

### 추가됨 (Added)
- **설정을 별도 페이지로 분리** + 상단 네비게이션(대시보드 / ⚙ 설정 / 📖 버전 기록).
- **버전 기록(version history)** 페이지: 현재 버전·스키마 + CHANGELOG 표시
  (`GET /api/changelog`).
- 설정 페이지에 **DB 저장 경로(읽기 전용)**, **로그 저장 경로**, **완료/오류 메일
  알림(SMTP)** 항목 추가. 스캔 종료 시 메일 발송(smtplib, 465=SSL/587=STARTTLS),
  log_path 지정 시 스캔 시작·종료를 파일에 기록. SMTP 비밀번호는 화면에 노출하지
  않고(마스킹) 빈값 저장 시 기존 값 유지.

### 변경됨 (Changed)
- 스캔 진행 중에는 「새 스캔 시작」 카드 전체를 숨긴다.

---

## [1.1.4] - 2026-06-08

### 추가됨 (Added)
- **프로세스별 CPU·메모리 표시**: 디스크 용량 계산에 쓰이는 스캐너 프로세스와
  du 자식 프로세스의 **CPU%** 를 대시보드에 표시(메모리 RSS 와 함께).
  스레드 동시 처리로 100%를 넘을 수 있다. (스키마 2: resource_samples 에
  scanner_cpu/du_cpu 추가, 구버전 DB 자동 마이그레이션)
- **관리 개요 표에 "소요 시간" 열** 추가(루트별 최신 스캔 경과 시간).

### 변경됨 (Changed)
- 스캔 진행 중에는 「새 스캔 시작」의 입력/탐색 영역(경로·옵션·마운트 선택·폴더
  탐색)을 **숨기고** 빨간 "● 스캔 중…" 버튼만 남긴다.

---

## [1.1.3] - 2026-06-08

### 변경됨 (Changed)
- 대시보드 레이아웃: **자원 모니터링(서버 메모리 / du 프로세스 메모리 / CPU·디스크)
  카드를 화면 맨 위로** 이동.

---

## [1.1.2] - 2026-06-08

### 수정됨 (Fixed)
- **탐색 중 진행 숫자가 멈춰 보이던 문제**: 파일이 매우 많은 한 디렉터리(NFS 등)를
  훑는 동안 총 파일 수/확인 용량이 그 디렉터리를 끝낼 때까지 갱신되지 않았다.
  이제 청크(약 2000개)마다 **실시간으로** 전역 카운터와 진행 상태를 갱신한다.

### 변경됨 (Changed)
- 기본 `scan_workers` 1 → **4** (NAS/NFS 에서 파일 stat 을 동시 처리해 가속).
  설정에서 더 높일수록 빨라진다.
- 대시보드: 스캔이 진행 중이면 「이 디렉터리 스캔 시작」 버튼이 **빨간 "● 스캔 중…"**
  으로 바뀐다(진행 중 표시).

---

## [1.1.1] - 2026-06-08

폐쇄망의 Python 3.6 환경(RHEL/CentOS 7 등)에서도 동작하도록 호환성 수정.

### 수정됨 (Fixed)
- **Python 3.6 호환**: 3.7+ 전용 문법/모듈을 제거 — `from __future__ import
  annotations`, PEP 604 `X | None`(→ `Optional`), PEP 585 `dict[...]`(→ `Dict`),
  `dataclasses`(→ 일반 클래스), `http.server.ThreadingHTTPServer`(→ 3.6 폴백),
  `subprocess(text=)`(→ `universal_newlines=`), `add_subparsers(required=)`
  (→ 속성 설정). 이제 **Python 3.6 이상**에서 실행된다(vermin 확인).
- **설치 호환**: 옛 setuptools(`<61`)와 Python 3.6 에서도 `pip install` 이 되도록
  PEP 621 `[project]` 대신 classic `setup.cfg`/`setup.py` 로 전환
  (`setuptools>=61` 강제 의존 제거). `python_requires>=3.6`.

> 폐쇄망에서는 설치 없이 압축을 풀어 `python3 -m isilon_usage ...` 로 바로
> 실행할 수 있습니다(표준 라이브러리만 사용).

---

## [1.1.0] - 2026-06-07

정확도·성능·관리 기능을 대폭 보강. DB 스키마 변경 없음(스키마 버전 1 유지).

### 추가됨 (Added)
- **하드링크 중복 제거**: native 백엔드가 `(st_dev, st_ino)` 로 하드링크를 1회만
  계산해 `du` 와 용량이 일치(스냅샷/하드링크 많은 NAS 정확도 향상).
- **stat 동시 처리(`scan_workers`)**: 디렉터리 내 파일 stat 을 스레드풀로 묶어
  처리해 NFS 대용량 스캔을 가속(청크 스트리밍으로 메모리는 그대로 최소).
- **디렉터리 드릴다운 + 검색**: 대시보드에서 디렉터리를 클릭해 하위로 파고드는
  트리 탐색과 경로 검색(`/api/children`, `/api/search`).
- **용량 추세 · 스캔 비교(diff)**: 루트별 조사 용량 추이 그래프와 두 스캔의
  디렉터리별 증감 비교(`/api/diff`).
- **오류 디렉터리 목록**: 접근 불가 디렉터리 목록/사유 표시(`/api/errors`).
- **보존 정책 / 정리**: 루트별 보관 수 또는 기간으로 오래된 스캔 자동/수동 정리
  (`retention_per_root` 설정, `/api/prune`, `prune` CLI, 스캔 삭제 `/api/scan/delete`).
- **완료 알림(웹훅)**: 스캔 완료/오류 시 웹훅(Slack 등) POST(`notify_webhook`).
- **예약 스캔(스케줄러)**: 경로별 주기 반복 스캔(`schedules` 설정 + 대시보드 편집기).
- **결과 내보내기**: 디렉터리 집계를 CSV/JSON 으로 스트리밍 다운로드(`/api/export`).
- **재개(resume)**: 중단된 스캔을 이어서 진행(`/api/scan/resume`, `resume` CLI).
- **패키징**: `pyproject.toml`(콘솔 스크립트 `isilon-usage`, 동적 버전, dashboard.html
  포함) + `Dockerfile`. `pip install .[monitor]` 로 설치 가능.
- **테스트/CI 강화**: 서버 API 통합 테스트(`tests/test_server.py`), 하드링크·권한
  거부 테스트, CI 에 ruff 린트 + 다중 Python 버전 + 서버 테스트 추가.

### 수정됨 (Fixed)
- 같은 루트를 같은 초에 두 번 스캔하면 per-run DB 파일명이 충돌해 두 스캔이 같은
  DB 를 공유하던 문제 수정(파일명에 고유 토큰 추가).

---

## [1.0.0] - 2026-06-07

초대용량 NAS(아이실론 등)의 디렉터리별 사용량을 **메모리 최소로** 조사하고,
진행 상황과 서버 자원을 **웹 대시보드**로 보여주는 도구의 첫 정식 릴리즈.

### 추가됨 (Added)

**스캔 엔진 (메모리 최소화)**
- 트리를 한 번에 처리하지 않는 2단계 스캔:
  - 1단계 탐색 — `os.scandir` 로 디렉터리를 한 번만 훑어 직접 파일의 용량/개수를
    기록하고, 방문 대기 목록을 메모리가 아닌 DB(`status='pending'`)에 둠.
  - 2단계 상향식 집계 — 최하위 레벨 → 같은 레벨 → 상위 레벨 순으로 재귀 용량을
    집계(파일을 두 번 훑지 않음).
- 두 가지 측정 백엔드:
  - `native` (기본) — 파이썬이 직접 측정(가장 빠르고 저메모리).
  - `du` — 디렉터리마다 시스템 `du` 를 별도 프로세스로 실행하고 그 프로세스의
    메모리를 대시보드에 표시.
- 용량 기준 선택(`--size-mode disk|apparent`), 마운트 경계 제한(`--one-file-system`).
- 심볼릭 링크 미추적, 접근 불가 항목은 "오류 디렉터리"로 집계.

**저장/관리 (버전·이력 관리)**
- 실행마다 별도의 per-run DB(`<data-dir>/scans/scan_<시각>_<경로>.db`)를 생성하여
  과거 스캔이 덮어써지지 않음.
- 관리(매니저) DB(`<data-dir>/manager.db`)에 모든 스캔의 요약을 모아 전체 용량을 관리.
  같은 루트는 "최신 스캔"만 합산하여 중복 합산을 방지.
- 각 스캔에 **생성한 앱 버전(app_version)** 을 기록하고, DB 스키마 버전을
  `PRAGMA user_version` 으로 관리(구버전 DB 컬럼 자동 보강).

**웹 대시보드 (표준 라이브러리, 외부 CDN 없음)**
- 현재 조사 중인 디렉터리, `처리/전체 디렉터리 수`, **확인된 사용량의 전체 디스크
  대비 %**, 단계, ETA 등 진행 상황 표시.
- 서버 자원 모니터링: **시스템 메모리 사용률**, **du/스캐너 프로세스 메모리**,
  CPU·load, 파일시스템 용량, 메모리 시계열 그래프, 용량 상위 디렉터리.
- 전체 용량 관리 개요(루트별 최신 합계) + 스캔 선택기.
- **웹에서 조사할 디렉터리 지정 후 스캔 시작/중지**:
  마운트 빠른 선택, 폴더 탐색기, 백엔드/옵션 선택, 진행 중 스캔 중지.
- `--mount-base` 로 웹에서 스캔 가능한 경로를 제한.
- **웹에서 모든 설정 보기/수정(설정 카드)**: 기본 백엔드/용량기준/한파일시스템,
  커밋 배치 크기, 자원 샘플링 주기, 허용 마운트 경로(mount_bases), 상위 N,
  대시보드 새로고침 주기를 편집해 `<data-dir>/settings.json` 에 저장(재시작에도 유지).
  `--lock-settings`(읽기 전용), `--reset-settings`(CLI 값으로 덮어쓰기) 제공.

**자원 모니터링**
- `psutil` 이 있으면 사용하고, 없으면 리눅스 `/proc` 폴백으로 동작(폐쇄망 대응).

**CLI**
- `run` — 초기 경로 스캔 + 대시보드(웹에서 추가 스캔 가능).
- `serve` — 대시보드 + 웹에서 디렉터리 지정 스캔(마운트 사용 시 권장).
- `scan` — 대시보드 없이 스캔만(배치용).
- `status` — 전체 관리 개요 + 스캔 상세를 콘솔에 출력.
- `version` / `--version` — 버전·환경 정보.

**API**
- `GET /api/scans`, `GET /api/status`, `GET /api/children`,
  `GET /api/browse`, `GET /api/mounts`,
  `POST /api/scan/start`, `POST /api/scan/stop`.

**릴리즈 자동화 (CI)**
- `.github/workflows/ci.yml`: 브랜치 push/PR 마다 Python 3.8/3.11/3.13 에서
  컴파일·CLI·테스트와 대시보드 JS 문법을 검증.
- `.github/workflows/release.yml`: `v*` 태그를 push 하면 테스트를 실행한 뒤
  CHANGELOG 의 해당 버전 섹션을 릴리즈 노트로 추출해 GitHub 릴리즈를 자동 생성.

**문서/테스트**
- 사용 설명서(`docs/USER_GUIDE.md`), README, 설치 가이드.
- 정확성 테스트(`tests/test_scanner.py`) — native/du 결과가 시스템 `du` 와 일치.
- 관리 DB 통합 테스트(`tests/test_manager.py`) — per-run 분리, 전체 집계, 중복 합산 방지.

### 알려진 제약 (Notes)
- 대시보드에는 인증이 없습니다. 신뢰망에서 쓰거나 `127.0.0.1` 바인딩 + SSH 터널을
  권장하며, 웹 스캔은 `--mount-base` 로 경로를 제한하세요.
- "확인된 사용량"은 하드링크/스파스 파일/블록 정렬 등으로 디스크 사용량과 정확히
  일치하지 않을 수 있습니다.

[1.1.1]: https://github.com/noainred/isilon_good/releases/tag/v1.1.1
[1.1.0]: https://github.com/noainred/isilon_good/releases/tag/v1.1.0
[1.0.0]: https://github.com/noainred/isilon_good/releases/tag/v1.0.0
