# 보안 가이드 (배포·운영 하드닝)

`isilon_usage` 는 **신뢰망에서 운영자가 직접 띄워 쓰는 내부 관리 도구**로
설계됐습니다. 강력한 다중 사용자 인증은 내장돼 있지 않으므로, 외부에 노출되는
환경이라면 아래 조치를 **반드시** 적용하세요.

## 위협 모델 (무엇을 가정하나)
- 대시보드/HTTP API 에 접근할 수 있는 사람은 **신뢰된 운영자**라고 가정합니다.
- 기본적으로 대시보드에는 다중 사용자 인증이 없고, HTTP(평문)로 동작합니다.
- 따라서 **인터넷/비신뢰망에 그대로 노출하면 안 됩니다.**

## 기본 동작 중 주의할 점
| 항목 | 기본값 | 위험 | 완화 |
|---|---|---|---|
| 바인드 호스트 | `--host 0.0.0.0` (모든 NIC) | 네트워크 누구나 접근 | `--host 127.0.0.1` + SSH 터널, 또는 방화벽. 비루프백+무인증 기동 시 **stderr 보안 경고** |
| 인증 | 없음(작업 비밀번호는 선택) | 누구나 보기·작업 | 아래 '작업 보호' + 리버스 프록시 인증 |
| 경로 탐색 `/api/browse` | **`mount_bases` 안만** 탐색(설정 시) | 디렉터리 구조 노출 | `--mount-base` 지정 시 그 하위만 나열. 미설정이면 신뢰망 한정·프록시 차단 |
| 스캔 경로 | `mount_bases` 비면 아무 경로 | 임의 디렉터리 스캔 | `--mount-base` 로 **허용 경로 제한** |
| 설정 편집 | 웹에서 가능 | 무단 변경 | `--lock-settings` 로 잠금 |
| 테스트 데이터 `gentest` | 파일을 씀 | 디스크 채우기 | 작업 보호 비밀번호 + 신뢰망 |
| 전송 | HTTP 평문 | 도청 | TLS 리버스 프록시 |
| 자동 업그레이드 | 옵트인(기본 꺼짐) | **감시 폴더 쓰기 권한자 = 코드 실행(RCE)** · 포탈→엣지 푸시 MITM | 아래 '자동 업그레이드 보안' |
| 포탈→엣지 비밀번호 관리 | api_token 인증(포탈 로그인 후) | 평문망에서 새 비밀번호 도청 | TLS 프록시/신뢰망 · 엣지마다 `api_token` 설정 |

## 권장 하드닝 (외부 노출 시 필수)
1. **로컬 바인드 + SSH 터널**
   ```bash
   python3 -m isilon_usage serve --host 127.0.0.1 --port 8765 ...
   ssh -L 8765:127.0.0.1:8765 user@서버    # 클라이언트에서
   ```
2. **허용 경로 제한**: `--mount-base /mnt/isilon` (여러 번 지정 가능).
3. **설정 잠금**: `--lock-settings` (웹 설정 편집 차단).
4. **로그인(작업 보호 비밀번호)**: 설정 화면 `🔒 작업 보호` 에서 지정 → **보기(GET)는 자유**,
   변경 작업(스캔 시작·설정·노드 등록 등 POST)에는 로그인. 로그인 세션은 **30분**(이후 자동
   로그아웃). 연속 실패가 잦으면 **30초 잠금** — *영구 잠금이 아니라 강한 비밀번호를 전제한
   속도 제한*이며 전역이므로 의도적 잠금 DoS 가 가능합니다(신뢰망 권장).
   - ⚠ 비밀번호는 **평문 HTTP 전송**(TLS 프록시 권장)이며, 기본은 `settings.json`·`info.MD`
     에 **평문 저장**입니다. 설정의 **암호화 저장**(PBKDF2 해시)을 켜면 평문을 남기지 않습니다.
   - ⚠ **보기(GET)는 인증 없이 공개**입니다(읽기 전용 모델). 포탈의 `/api/portal/overview`·
     `nodes`·`agent-bundle` 등은 로그인 없이 조회되어 **모니터링 데이터·노드 토폴로지가
     노출**됩니다. 민감하면 신뢰망/프록시로 막으세요.
5. **TLS + 인증 프록시**: 외부 노출이 꼭 필요하면 nginx/Caddy 등 앞단에서
   HTTPS + Basic/SSO 인증을 두고, 백엔드는 `127.0.0.1` 로만 띄우세요.
6. **읽기 전용 마운트**: 조사 대상은 `mount -o ro` 로 마운트(사고 방지).
7. **최소 권한 계정**으로 실행(마운트 읽기 권한만; 루트 불필요).
8. **로그인(접근 제어)**: 설정에서 작업 보호 비밀번호를 지정하면 보기를 제외한 모든 변경
   작업에 로그인이 필요합니다(스캐너·포탈 공통). 비밀번호 **암호화 저장** 옵션 권장. 외부
   노출 시 기동 직후 비밀번호를 설정하세요.

## 자동 업그레이드 보안 (감시 폴더 + 전 노드 푸시)
이 기능은 **코드를 자동 적용·재시작**하므로 본질적으로 강력합니다(기본은 꺼짐, 옵트인).
- **감시 폴더는 신뢰·제한 폴더여야 합니다.** 그 폴더에 `isilon_usage-*.tar.gz/.zip` 을 쓸 수
  있는 사람은 **서비스 계정 권한으로 임의 코드를 실행**할 수 있습니다(**감시 폴더 경로는** 버전만 보고
  적용 — 서명·체크섬 검증 없음). 폴더 권한을 전용 계정 전용(예: `chmod 700`)으로 제한하고, 공유
  마운트·월드라이터블 경로를 쓰지 마세요.
- **URL/미러 자동 업그레이드는 SHA-256 무결성 검증을 합니다.** `versions.json` 의 `sha256`(및 `sha256_zip`)
  과 내려받은 아카이브를 대조해 **불일치 시 거부**합니다(변조·탈취된 미러 차단 — `upgrade.py`, `make_release.py`
  가 매니페스트에 기록). 단 이는 **매니페스트 대비 무결성**이지 암호 서명(진위)은 아닙니다 — `versions.json`
  을 제공하는 미러/전송 경로 자체는 신뢰할 수 있어야 하며, 그래서 **TLS/신뢰망**이 여전히 필요합니다.
- **포탈→엣지 코드 푸시는 HTTP** 로 전송됩니다. 비신뢰망에서는 MITM 이 코드/토큰을 가로채
  악성 코드를 주입할 수 있으니, **TLS 프록시 또는 신뢰망**에서만 쓰고 엣지마다 `api_token`
  을 설정하세요(미설정 엣지는 푸시 수신을 거부합니다).
- 적용 시 **기존 코드는 백업**(`isilon_usage.bak.<시각>`)되고, 업그레이드 이벤트는 감사
  로그에 남습니다. 손상된/거대 아카이브로 인한 과도한 메모리 사용을 막기 위해 적용 시 크기
  상한을 둡니다.

## 민감 파일
- `info.MD` — 작업 비밀번호 평문 기록(권한 600). **저장소에 커밋 금지**
  (이미 `.gitignore` 처리). 백업 시 접근 권한에 유의. 설정에서 **비밀번호 암호화 저장**을
  켜면 평문 대신 PBKDF2 해시로 저장하고 `info.MD` 는 기록하지 않습니다.
- `<data-dir>/settings.json` — 스토리지/SMTP/토큰 비밀번호가 들어갈 수 있으니
  파일 권한(600/640)과 디렉터리 접근을 제한하세요.
- `<data-dir>/portal_settings.json` — 포탈 로그인 비밀번호(평문 또는 PBKDF2 해시) 저장. 권한 제한.
- `<data-dir>/audit.log` — 로그인·변경 작업 감사 기록(출처 IP 포함). 접근 권한에 유의.

## 코드 근거 점검 결과 (v1.99.23 기준) — 무엇을 확인했나
정적 코드 검토 + 동작 확인으로 인증/토큰/저장/추출/주입/경로를 점검했다. **사실만** 적는다.

### ✅ 확인된 강점 (구현 결함은 적절히 방어됨)
- **비밀번호 해시**: PBKDF2-HMAC-SHA256·200,000회·16B 솔트(`auth.py`), 검증은 **상수시간**(`hmac.compare_digest`).
- **토큰 비교 상수시간**: `api_token`도 `hmac.compare_digest`(`server.py:_authorized`).
- **무차별 대입 완화**: 로그인 5회 실패 → 30초 잠금(단, 전역·IP별 아님 → 의도적 잠금 DoS 가능).
- **업그레이드 번들 안전**: 멤버명 경로 탈출(`..`) 차단 + `isilon_usage/` 하위만 수용(`upgrade.py:_accept_member`),
  **tar/zip 폭탄 상한**(200MB·20,000개) → zip‑slip·압축폭탄 방어.
- **비밀 마스킹**: GET 설정 응답에서 비밀번호·토큰을 빈 값으로 가리고 `*_set` 만 노출(`_public_settings`).
- **민감 대량 read 보호**: `/api/dbexport`(스캔 DB 전체)는 `api_token` 인증 필요(`server.py` `_authorized`). *단, api_token 미설정이면 공개.*
- **SSH 실행에 셸 주입 없음**: `subprocess.run` 에 **argv 리스트**(no `shell=True`)로 전달(`portal.py`).
- **업그레이드 무결성 검증**: URL/미러 자동 업그레이드는 **SHA-256** 대조 후 적용(`upgrade.py`, `versions.json` 매니페스트) → 변조 미러 차단(무결성; 서명=진위는 아님).
- **출력 이스케이프(저장형 XSS 방어)**: 원격/사용자 유래 값(경로·버전·URL·오류·표)을 `escHtml`/`_he` 로 이스케이프하고 다수는 `textContent` 로 렌더(`dashboard.html`·`portal.html`). 무인증 `/api/browse`·포탈 browse 는 작업 비밀번호 게이트로 마운트 밖 열람 차단.
- **공급망 표면 최소**: 외부 의존성 0(표준 라이브러리만; `psutil` 은 선택).

### 🔐 포탈→엣지 비밀번호 관리(v1.79.0) 점검
- 엣지 `/api/op-password` 는 **op_password 가 아니라 api_token(X-Auth-Token)** 으로 인증한다(원격 업그레이드와 같은 권한 모델).
  그래서 포탈이 엣지 비밀번호를 몰라도 설정/변경/해제할 수 있다. **api_token 미등록 노드는 푸시를 거부**한다.
- 저장 규칙은 기존 설정 흐름과 동일(암호화 여부·info.MD). **포탈 로그인 후에만** 호출 가능(변경 게이트).
- ⚠ **세션 토큰은 비밀번호 변경으로 무효화되지 않는다**(엣지·포탈 공통). 기존 세션은 TTL(기본 30분)까지 유효 —
  즉시 차단이 필요하면 **프로세스 재시작**.

### ⚠ 남는 위험은 대부분 ‘배치 전제’
암호·주입·추출 같은 구현 결함은 막혀 있다. 남는 위험은 **평문 HTTP·읽기 공개**라는 전제에서 온다 —
위 ‘권장 하드닝’(TLS 프록시, `mount_bases`, `api_token`, 암호화 저장, 신뢰망)으로 닫는다.

### 점검 범위·한계 (정직 고지)
정적 검토 + 동작 확인 수준이다(외부 침투 테스트·퍼징 안 함). 실 Isilon 클러스터·대규모 멀티 DC 검증은 없다(랩 한정).

## 취약점 신고
보안 문제를 발견하면 공개 이슈 대신 저장소 소유자에게 비공개로 알려 주세요.
